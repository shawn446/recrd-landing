---
// Hero Section - CSS Grid with SVG Path Overlay
---

<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>recrd - AI agents that close deals</title>
  
  <style>
    /* Fonts */
    @font-face {
      font-family: 'Roobert';
      src: url('/fonts/RoobertCollectionVF-TRIAL.woff2') format('woff2');
      font-weight: 100 900;
      font-style: normal;
      font-display: swap;
    }
    @font-face {
      font-family: 'Fraktion Mono';
      src: url('/fonts/PPFraktionMono-Regular.otf') format('opentype');
      font-weight: 400;
      font-style: normal;
      font-display: swap;
    }

    :root {
      --font: 'Roobert', -apple-system, BlinkMacSystemFont, sans-serif;
      --mono: 'Fraktion Mono', 'SF Mono', monospace;
      --mint: #46FCAF;
      --purple: #A678F4;
      --cyan: #02C0FF;
      --coral: #FF4F5E;
      
      /* Grid config */
      --cell-size: 120px;
      --cols: 7;
      --rows: 8;
    }

    [data-theme="dark"] {
      --bg: #0A0A0A;
      --text: #ffffff;
      --text-muted: #888888;
      --grid-line: rgba(255,255,255,0.1);
    }

    [data-theme="light"] {
      --bg: #fafafa;
      --text: #0a0a0a;
      --text-muted: #666666;
      --grid-line: rgba(0,0,0,0.1);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html, body {
      overflow-x: hidden;
    }

    body {
      font-family: var(--font);
      background: var(--bg);
      color: var(--text);
      min-height: 100vh;
    }

    /* NAV */
    nav {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      height: 72px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0 48px;
      z-index: 100;
      background: var(--bg);
      border-bottom: 1px solid var(--grid-line);
    }

    .nav-logo {
      display: flex;
      align-items: center;
      gap: 12px;
      text-decoration: none;
      transition: opacity 0.2s ease;
    }

    .nav-logo:hover { opacity: 0.8; }

    .nav-logo svg {
      width: 32px;
      height: 32px;
    }

    .nav-wordmark {
      font-weight: 600;
      font-size: 18px;
      color: var(--text);
    }

    .nav-links {
      display: flex;
      gap: 8px;
      list-style: none;
    }

    .nav-links a {
      color: var(--text-muted);
      text-decoration: none;
      font-size: 14px;
      font-weight: 500;
      padding: 10px 16px;
      transition: all 0.2s ease;
    }

    .nav-links li:nth-child(1) a:hover { background: var(--cyan); color: #0a0a0a; }
    .nav-links li:nth-child(2) a:hover { background: var(--mint); color: #0a0a0a; }
    .nav-links li:nth-child(3) a:hover { background: var(--purple); color: #fff; }
    .nav-links li:nth-child(4) a:hover { background: var(--coral); color: #fff; }

    .nav-cta {
      background: var(--text);
      color: var(--bg);
      padding: 12px 24px;
      font-weight: 500;
      font-size: 14px;
      text-decoration: none;
      transition: all 0.2s ease;
    }

    .nav-cta:hover {
      background: var(--mint);
      color: #0a0a0a;
    }

    /* HERO */
    .hero {
      min-height: 100vh;
      display: flex;
      align-items: center;
      padding: 72px 0 0 48px;
      position: relative;
    }

    .hero-content {
      max-width: 480px;
      z-index: 10;
      padding: 48px 0;
    }

    .hero-headline {
      font-size: clamp(48px, 8vw, 88px);
      font-weight: 700;
      line-height: 0.95;
      letter-spacing: -0.035em;
      margin-bottom: 24px;
    }

    .hero-headline em {
      font-style: normal;
      color: var(--mint);
    }

    [data-theme="light"] .hero-headline em {
      color: var(--purple);
    }

    .hero-sub {
      font-size: clamp(24px, 3.5vw, 38px);
      font-weight: 500;
      line-height: 1.2;
      margin-bottom: 20px;
      letter-spacing: -0.02em;
    }

    .hero-body {
      font-size: 18px;
      font-weight: 300;
      color: var(--text-muted);
      line-height: 1.6;
      margin-bottom: 40px;
    }

    .hero-buttons {
      display: flex;
      gap: 16px;
    }

    .btn {
      padding: 16px 32px;
      font-weight: 500;
      font-size: 15px;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      gap: 12px;
      transition: all 0.2s ease;
      cursor: pointer;
    }

    .btn-primary {
      background: #fff;
      color: #0a0a0a;
    }

    .btn-primary:hover {
      background: var(--mint);
      transform: translateY(-2px);
    }

    .btn-secondary {
      background: transparent;
      color: var(--text);
      border: 1px solid var(--grid-line);
    }

    .btn-secondary:hover {
      background: var(--cyan);
      border-color: var(--cyan);
      color: #0a0a0a;
      transform: translateY(-2px);
    }

    /* ========================================
       GRID SYSTEM
       ======================================== */
    
    .grid-wrapper {
      position: absolute;
      top: 72px;
      right: 0;
      width: calc(var(--cell-size) * var(--cols));
      height: calc(var(--cell-size) * var(--rows));
      display: grid;
      grid-template-columns: repeat(var(--cols), var(--cell-size));
      grid-template-rows: repeat(var(--rows), var(--cell-size));
    }

    .grid-cell {
      border: 1px solid var(--grid-line);
      margin: -0.5px;
    }

    /* SVG overlay - explicit dimensions to match grid */
    .path-overlay {
      position: absolute;
      top: 0;
      left: 0;
      width: calc(var(--cell-size) * var(--cols));
      height: calc(var(--cell-size) * var(--rows));
      pointer-events: none;
      z-index: 2;
    }

    .flow-path {
      fill: none;
      stroke-width: 2;
      stroke-linecap: round;
      stroke-linejoin: round;
      filter: drop-shadow(0 0 8px currentColor);
    }

    /* ========================================
       CARDS
       ======================================== */
    
    .card {
      position: absolute;
      display: flex;
      flex-direction: column;
      padding: 20px 24px;
      opacity: 0;
      transform: scale(0.95);
      z-index: 5;
    }

    .card-header {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 12px;
    }

    .card-icon {
      width: 22px;
      height: 22px;
    }

    .card-icon svg {
      width: 100%;
      height: 100%;
    }

    .card-badge {
      font-family: var(--mono);
      font-size: 9px;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      padding: 5px 10px;
    }

    .card-title {
      font-size: 18px;
      font-weight: 600;
      line-height: 1.2;
      margin-bottom: 6px;
    }

    .card-desc {
      font-size: 13px;
      line-height: 1.4;
      opacity: 0.85;
    }

    .card-meta {
      font-family: var(--mono);
      font-size: 11px;
      margin-top: auto;
      padding-top: 8px;
      opacity: 0.7;
    }

    /* Card colors */
    .card.mint {
      background: var(--mint);
      color: #0a0a0a;
      box-shadow: 0 4px 30px rgba(70, 252, 175, 0.3);
    }
    .card.mint .card-badge { background: rgba(0,0,0,0.1); }
    .card.mint .card-icon svg { stroke: #0a0a0a; fill: none; }

    .card.purple {
      background: var(--purple);
      color: #fff;
      box-shadow: 0 4px 30px rgba(166, 120, 244, 0.35);
    }
    .card.purple .card-badge { background: rgba(255,255,255,0.2); }
    .card.purple .card-icon svg { stroke: #fff; fill: none; }

    .card.cyan {
      background: var(--cyan);
      color: #0a0a0a;
      box-shadow: 0 4px 30px rgba(2, 192, 255, 0.3);
    }
    .card.cyan .card-badge { background: rgba(0,0,0,0.1); }
    .card.cyan .card-icon svg { stroke: #0a0a0a; fill: none; }

    .card.coral {
      background: var(--coral);
      color: #fff;
      box-shadow: 0 4px 30px rgba(255, 79, 94, 0.35);
    }
    .card.coral .card-badge { background: rgba(255,255,255,0.2); }
    .card.coral .card-icon svg { stroke: #fff; fill: none; }

    /* Card positions - calculated from cell-size 
       Each card snaps to grid intersections */
    
    #card1 {
      top: calc(var(--cell-size) * 0);
      left: calc(var(--cell-size) * 0);
      width: calc(var(--cell-size) * 2.5);
      height: calc(var(--cell-size) * 1.5);
    }

    #card2 {
      top: calc(var(--cell-size) * 2);
      left: calc(var(--cell-size) * 2);
      width: calc(var(--cell-size) * 2.5);
      height: calc(var(--cell-size) * 1.5);
    }

    #card3 {
      top: calc(var(--cell-size) * 4);
      left: calc(var(--cell-size) * 4.5);
      width: calc(var(--cell-size) * 2.5);
      height: calc(var(--cell-size) * 1.5);
    }

    #card4 {
      top: calc(var(--cell-size) * 6);
      left: calc(var(--cell-size) * 4.5);
      width: calc(var(--cell-size) * 2.5);
      height: calc(var(--cell-size) * 1.5);
    }

    #card5 {
      top: calc(var(--cell-size) * 6);
      left: calc(var(--cell-size) * 0);
      width: calc(var(--cell-size) * 2.5);
      height: calc(var(--cell-size) * 1.5);
    }

    /* Theme toggle */
    .theme-toggle {
      position: fixed;
      bottom: 24px;
      left: 50%;
      transform: translateX(-50%);
      display: flex;
      background: var(--bg);
      border: 1px solid var(--grid-line);
      z-index: 1000;
    }

    .theme-toggle button {
      padding: 10px 20px;
      background: none;
      border: none;
      color: var(--text-muted);
      font-size: 13px;
      cursor: pointer;
      transition: all 0.2s ease;
    }

    .theme-toggle button:hover:not(.active) {
      color: var(--text);
      background: var(--grid-line);
    }

    .theme-toggle button.active {
      background: var(--text);
      color: var(--bg);
    }

    /* RESPONSIVE */
    @media (max-width: 1600px) {
      :root { --cell-size: 100px; }
    }

    @media (max-width: 1400px) {
      :root { --cell-size: 85px; }
      .card { padding: 16px 20px; }
      .card-title { font-size: 16px; }
      .card-desc { font-size: 12px; }
    }

    @media (max-width: 1200px) {
      :root { --cell-size: 70px; }
      .card { padding: 12px 16px; }
      .card-header { gap: 8px; margin-bottom: 8px; }
      .card-badge { font-size: 8px; padding: 4px 8px; }
      .card-title { font-size: 14px; margin-bottom: 4px; }
      .card-desc { font-size: 11px; }
      .card-meta { display: none; }
      .card-icon { width: 18px; height: 18px; }
    }

    @media (max-width: 1000px) {
      :root { --cell-size: 55px; }
      .card { padding: 10px 12px; }
      .card-desc { display: none; }
      .card-title { font-size: 11px; }
    }

    @media (max-width: 900px) {
      .hero {
        flex-direction: column;
        padding: 100px 24px 48px;
        min-height: auto;
      }
      .hero-content {
        max-width: 100%;
        text-align: center;
        margin-bottom: 40px;
      }
      .hero-buttons { justify-content: center; }
      .grid-wrapper {
        position: relative;
        width: 100%;
        max-width: 500px;
        margin: 0 auto;
        --cell-size: min(60px, calc((100vw - 48px) / 7));
        height: calc(var(--cell-size) * var(--rows));
      }
      .nav-links { display: none; }
      nav { padding: 0 24px; }
    }

    @media (max-width: 600px) {
      .hero-headline { font-size: 36px; }
      .hero-sub { font-size: 20px; }
      .hero-body { font-size: 15px; }
      .hero-buttons { flex-direction: column; }
      .btn { width: 100%; justify-content: center; }
      :root { --cell-size: calc((100vw - 48px) / 7); }
      .card { padding: 6px 8px; }
      .card-badge { font-size: 6px; padding: 2px 4px; }
      .card-title { font-size: 9px; }
      .card-icon { width: 12px; height: 12px; }
      .card-header { gap: 4px; margin-bottom: 4px; }
    }
  </style>
</head>

<body>
  <nav>
    <a href="/" class="nav-logo">
      <svg viewBox="0 0 52 52" fill="none">
        <rect y="16" width="28" height="28" fill="#A678F4"/>
        <rect x="16" width="28" height="28" rx="6" fill="#46FCAF"/>
      </svg>
      <span class="nav-wordmark">recrd</span>
    </a>
    <ul class="nav-links">
      <li><a href="#">Features</a></li>
      <li><a href="#">Pricing</a></li>
      <li><a href="#">Docs</a></li>
      <li><a href="#">Blog</a></li>
    </ul>
    <a href="#" class="nav-cta">Dashboard</a>
  </nav>

  <section class="hero">
    <div class="hero-content">
      <h1 class="hero-headline">AI agents that <em>close deals</em></h1>
      <p class="hero-sub">One call. Six agents. Zero setup.</p>
      <p class="hero-body">Record your sales calls and let AI handle the follow-ups, CRM updates, and coaching insights.</p>
      <div class="hero-buttons">
        <a href="#" class="btn btn-primary">Get Started Free</a>
        <a href="#" class="btn btn-secondary">Watch Demo</a>
      </div>
    </div>

    <div class="grid-wrapper">
      <!-- Grid cells for lines -->
      <div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div>
      <div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div>
      <div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div>
      <div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div>
      <div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div>
      <div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div>
      <div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div>
      <div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div><div class="grid-cell"></div>

      <!-- SVG Paths - viewBox matches grid: 7 cols × 8 rows = 700 × 800 units -->
      <svg class="path-overlay" viewBox="0 0 700 800" preserveAspectRatio="none">
        <!-- Card positions in grid units (multiply by 100 for viewBox coords):
             Card1: (0,0) to (250,150) → right edge x=250, bottom y=150
             Card2: (200,200) to (450,350) → left x=200, top y=200  
             Card3: (450,400) to (700,550) → left x=450, top y=400
             Card4: (450,600) to (700,750) → left x=450, top y=600
             Card5: (0,600) to (250,750) → right x=250
        -->
        
        <!-- Path 1: Card1 right-bottom corner → down → left to Card2 top-left -->
        <path id="path1" class="flow-path" stroke="#46FCAF"
              d="M 250,150 L 250,200 L 200,200"/>
        
        <!-- Path 2: Card2 right edge → right → down to Card3 top -->
        <path id="path2" class="flow-path" stroke="#A678F4"
              d="M 450,275 L 500,275 L 500,400 L 450,400"/>
        
        <!-- Path 3: Card3 bottom → down to Card4 top -->
        <path id="path3" class="flow-path" stroke="#02C0FF"
              d="M 575,550 L 575,600"/>
        
        <!-- Path 4: Card4 left → left to Card5 right -->
        <path id="path4" class="flow-path" stroke="#FF4F5E"
              d="M 450,675 L 250,675"/>
      </svg>

      <!-- Cards -->
      <div class="card mint" id="card1">
        <div class="card-header">
          <div class="card-icon">
            <svg viewBox="0 0 24 24" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
            </svg>
          </div>
          <span class="card-badge">Trigger</span>
        </div>
        <span class="card-title">Call completed</span>
        <span class="card-desc">Demo with Acme Corp</span>
        <span class="card-meta">32 min · Zoom</span>
      </div>

      <div class="card purple" id="card2">
        <div class="card-header">
          <div class="card-icon">
            <svg viewBox="0 0 24 24" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M2 12h2"/><path d="M6 8v8"/><path d="M10 4v16"/><path d="M14 6v12"/><path d="M18 9v6"/><path d="M22 12h-2"/>
            </svg>
          </div>
          <span class="card-badge">Processing</span>
        </div>
        <span class="card-title">recrd analyzes</span>
        <span class="card-desc">Extracting insights</span>
        <span class="card-meta">6 agents deployed</span>
      </div>

      <div class="card cyan" id="card3">
        <div class="card-header">
          <div class="card-icon">
            <svg viewBox="0 0 24 24" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <line x1="18" y1="20" x2="18" y2="10"/><line x1="12" y1="20" x2="12" y2="4"/><line x1="6" y1="20" x2="6" y2="14"/>
            </svg>
          </div>
          <span class="card-badge">CRM Agent</span>
        </div>
        <span class="card-title">Pipeline updated</span>
        <span class="card-desc">Deal stage → Negotiation</span>
        <span class="card-meta">HubSpot synced</span>
      </div>

      <div class="card coral" id="card4">
        <div class="card-header">
          <div class="card-icon">
            <svg viewBox="0 0 24 24" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"/><path d="M13.73 21a2 2 0 0 1-3.46 0"/>
            </svg>
          </div>
          <span class="card-badge">Risk Alert</span>
        </div>
        <span class="card-title">Manager notified</span>
        <span class="card-desc">Budget concern flagged</span>
        <span class="card-meta">Slack #deals</span>
      </div>

      <div class="card mint" id="card5">
        <div class="card-header">
          <div class="card-icon">
            <svg viewBox="0 0 24 24" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="m3 11 18-5v12L3 13v-2z"/><path d="M11.6 16.8a3 3 0 1 1-5.8-1.6"/>
            </svg>
          </div>
          <span class="card-badge">Content Agent</span>
        </div>
        <span class="card-title">Marketing alerted</span>
        <span class="card-desc">Competitor quote captured</span>
        <span class="card-meta">Case study opp</span>
      </div>
    </div>
  </section>

  <div class="theme-toggle">
    <button id="btn-dark" class="active" onclick="setTheme('dark')">Dark</button>
    <button id="btn-light" onclick="setTheme('light')">Light</button>
  </div>

  <script>
    import { gsap } from 'gsap';
    import Lenis from 'lenis';

    function setTheme(theme) {
      document.documentElement.setAttribute('data-theme', theme);
      document.getElementById('btn-dark')?.classList.toggle('active', theme === 'dark');
      document.getElementById('btn-light')?.classList.toggle('active', theme === 'light');
    }
    window.setTheme = setTheme;

    const lenis = new Lenis({
      duration: 1.2,
      easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t)),
      smoothWheel: true,
    });

    function raf(time) {
      lenis.raf(time);
      requestAnimationFrame(raf);
    }
    requestAnimationFrame(raf);

    function createTimeline() {
      const paths = document.querySelectorAll('.flow-path');
      
      paths.forEach(path => {
        const length = path.getTotalLength();
        path.style.strokeDasharray = length;
        path.style.strokeDashoffset = length;
      });

      gsap.set('.card', { opacity: 0, scale: 0.95, y: 10 });

      const tl = gsap.timeline({
        repeat: -1,
        repeatDelay: 3,
        onRepeat: () => {
          paths.forEach(path => {
            const length = path.getTotalLength();
            gsap.set(path, { strokeDashoffset: length });
          });
          gsap.set('.card', { opacity: 0, scale: 0.95, y: 10 });
        }
      });

      tl.to('#card1', { opacity: 1, scale: 1, y: 0, duration: 0.6, ease: 'power2.out' });
      tl.to('#path1', { strokeDashoffset: 0, duration: 1.2, ease: 'power1.inOut' }, '-=0.1');
      tl.to('#card2', { opacity: 1, scale: 1, y: 0, duration: 0.6, ease: 'power2.out' }, '-=0.3');
      tl.to({}, { duration: 0.3 });
      tl.to('#path2', { strokeDashoffset: 0, duration: 1, ease: 'power1.inOut' });
      tl.to('#card3', { opacity: 1, scale: 1, y: 0, duration: 0.6, ease: 'power2.out' }, '-=0.2');
      tl.to('#path3', { strokeDashoffset: 0, duration: 0.6, ease: 'power1.inOut' }, '-=0.1');
      tl.to('#card4', { opacity: 1, scale: 1, y: 0, duration: 0.6, ease: 'power2.out' }, '-=0.2');
      tl.to({}, { duration: 0.2 });
      tl.to('#path4', { strokeDashoffset: 0, duration: 1.4, ease: 'power1.inOut' });
      tl.to('#card5', { opacity: 1, scale: 1, y: 0, duration: 0.6, ease: 'power2.out' }, '-=0.4');
      tl.to({}, { duration: 2 });

      return tl;
    }

    document.addEventListener('DOMContentLoaded', () => {
      setTimeout(createTimeline, 100);
    });
  </script>
</body>
</html>
