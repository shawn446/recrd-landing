---
import Nav from '../../components/Nav.astro';
import Footer from '../../components/Footer.astro';
import ThemeToggle from '../../components/ThemeToggle.astro';
import BaseHead from '../../components/BaseHead.astro';
---
<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
  <BaseHead title="recrd for Marketing - Hear What Prospects Actually Say" description="Real voice-of-customer insights from sales calls to fuel campaigns, content, and competitive positioning." />
  <style>
    .hero { padding: 160px 48px 100px; max-width: 1200px; margin: 0 auto; display: grid; grid-template-columns: 1fr 1fr; gap: 80px; align-items: center; }
    .hero-content { max-width: 540px; }
    .hero-badge { display: inline-flex; padding: 8px 16px; background: var(--bg-card); border: 1px solid var(--border); font-family: var(--mono); font-size: 12px; text-transform: uppercase; letter-spacing: 0.1em; margin-bottom: 24px; color: var(--coral); }
    .hero-headline { font-size: 56px; font-weight: 350; line-height: 1.1; letter-spacing: -0.03em; margin-bottom: 24px; }
    .hero-headline em { font-style: normal; color: var(--coral); }
    .hero-sub { font-size: 18px; color: var(--text-muted); line-height: 1.6; margin-bottom: 40px; }
    .hero-cta { display: inline-flex; align-items: center; gap: 12px; padding: 18px 32px; font-weight: 500; font-size: 15px; text-decoration: none; position: relative; overflow: hidden; background: transparent; color: var(--text); border: 1px solid var(--btn-outline); }
    .hero-cta::before { content: ''; position: absolute; top: 0; left: 0; width: 0; height: 100%; background: var(--coral); transition: width 0.3s ease; z-index: -1; }
    .hero-cta:hover::before { width: 100%; }
    .hero-cta:hover { color: #fff; border-color: var(--coral); }
    .hero-cta-arrow { display: inline-flex; transition: transform 0.2s ease; }
    .hero-cta-arrow img { width: 16px; height: 16px; }
    .hero-cta:hover .hero-cta-arrow { transform: translateX(4px); }
    .hero-cta-arrow .chevron-hover { display: none; }
    .hero-cta:hover .chevron-default { display: none; }
    .hero-cta:hover .chevron-hover { display: block; }

    .hero-visual { background: var(--bg-card); border: 1px solid var(--border); padding: 32px; }
    .hero-mock-header { display: flex; align-items: center; justify-content: space-between; padding-bottom: 16px; border-bottom: 1px solid var(--border); margin-bottom: 20px; }
    .hero-mock-title { font-weight: 600; font-size: 14px; }
    .hero-mock-badge { font-family: var(--mono); font-size: 10px; padding: 6px 12px; background: var(--coral); color: #fff; text-transform: uppercase; }
    .insight-item { padding: 14px 0; border-bottom: 1px solid var(--border); }
    .insight-item:last-child { border-bottom: none; }
    .insight-label { font-family: var(--mono); font-size: 11px; color: var(--text-muted); text-transform: uppercase; margin-bottom: 6px; }
    .insight-quote { font-size: 14px; font-style: italic; line-height: 1.5; }
    .insight-count { font-family: var(--mono); font-size: 11px; color: var(--coral); margin-top: 6px; }
    .pain-section { padding: 100px 48px; border-top: 1px solid var(--border); background: var(--bg-alt); }
    .pain-header { text-align: center; max-width: 600px; margin: 0 auto 64px; }
    .pain-label { font-family: var(--mono); font-size: 12px; text-transform: uppercase; letter-spacing: 0.1em; color: var(--coral); margin-bottom: 16px; }
    .pain-headline { font-size: 36px; font-weight: 350; line-height: 1.2; }
    .pain-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1px; background: var(--border); max-width: 1000px; margin: 0 auto; }
    .pain-card { background: var(--bg); padding: 40px 32px; text-align: center; }
    .pain-stat { font-size: 48px; font-weight: 300; color: var(--coral); margin-bottom: 12px; font-family: var(--mono); }
    .pain-desc { font-size: 15px; color: var(--text-muted); line-height: 1.5; }
    .solution-section { padding: 120px 48px; border-top: 1px solid var(--border); }
    .solution-header { text-align: center; max-width: 600px; margin: 0 auto 80px; }
    .solution-label { font-family: var(--mono); font-size: 12px; text-transform: uppercase; letter-spacing: 0.1em; color: var(--coral); margin-bottom: 16px; }
    .solution-headline { font-size: 36px; font-weight: 350; line-height: 1.2; margin-bottom: 16px; }
    .solution-sub { font-size: 16px; color: var(--text-muted); }
    .solution-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 48px; max-width: 1000px; margin: 0 auto; }
    .solution-card { padding: 40px; background: var(--bg-card); border: 1px solid var(--border); }
    .solution-icon { width: 48px; height: 48px; margin-bottom: 24px; }
    .solution-icon img { width: 100%; height: 100%; object-fit: contain; }
    .solution-card-soon { position: relative; }
    .solution-badge { position: absolute; top: 16px; right: 16px; font-family: var(--mono); font-size: 10px; text-transform: uppercase; letter-spacing: 0.05em; padding: 4px 10px; background: var(--border); color: var(--text-muted); }
    .solution-title { font-size: 20px; font-weight: 600; margin-bottom: 12px; }
    .solution-desc { font-size: 15px; color: var(--text-muted); line-height: 1.6; }
    .quote-section { padding: 100px 48px; border-top: 1px solid var(--border); background: var(--bg-alt); }
    .quote-container { max-width: 800px; margin: 0 auto; text-align: center; }
    .quote-mark { font-size: 80px; line-height: 1; color: var(--coral); opacity: 0.3; margin-bottom: -20px; }
    .quote-text { font-size: 28px; font-weight: 350; line-height: 1.5; margin-bottom: 32px; }
    .quote-author { font-size: 15px; color: var(--text-muted); }
    .quote-author strong { color: var(--text); font-weight: 600; }
    .cta-section { padding: 120px 48px; text-align: center; border-top: 1px solid var(--border); }
    .cta-headline { font-size: 40px; font-weight: 350; margin-bottom: 16px; }
    .cta-sub { font-size: 18px; color: var(--text-muted); margin-bottom: 40px; }
    .btn { display: inline-flex; align-items: center; gap: 12px; padding: 18px 28px; font-weight: 500; font-size: 15px; text-decoration: none; position: relative; overflow: hidden; background: transparent; color: var(--text); border: 1px solid var(--btn-outline); }
    .btn::before { content: ''; position: absolute; top: 0; left: 0; width: 0; height: 100%; background: var(--coral); transition: width 0.3s ease; z-index: -1; }
    .btn:hover::before { width: 100%; }
    .btn:hover { color: #fff; border-color: var(--coral); }
    .btn-arrow { display: inline-flex; transition: transform 0.2s ease; }
    .btn-arrow img { width: 16px; height: 16px; }
    .btn:hover .btn-arrow { transform: translateX(4px); }
    .btn-arrow .chevron-hover { display: none; }
    .btn:hover .chevron-default { display: none; }
    .btn:hover .chevron-hover { display: block; }

    @media (max-width: 1000px) { .hero { grid-template-columns: 1fr; gap: 48px; text-align: center; } .solution-grid { grid-template-columns: 1fr; } }
    @media (max-width: 900px) { .hero { padding: 140px 24px 80px; } .hero-headline { font-size: 40px; } .pain-section { padding: 80px 24px; } .pain-grid { grid-template-columns: 1fr; } .solution-section { padding: 80px 24px; } .quote-section { padding: 80px 24px; } .cta-section { padding: 80px 24px; } }
  </style>
</head>
<body>
  <Nav />
  <section class="hero">
    <div class="hero-content">
      <div class="hero-badge">For Marketing</div>
      <h1 class="hero-headline">Hear what prospects <em>actually say</em></h1>
      <p class="hero-sub">Stop guessing what messaging resonates. Get real voice-of-customer insights from sales calls to fuel your campaigns and competitive positioning.</p>
      <a href="#" class="hero-cta">Book a Demo <span class="hero-cta-arrow"><img src="/icons/chevron-green.svg" alt="" class="chevron-default" /><img src="/icons/chevron-white.svg" alt="" class="chevron-hover" /></span></a>
    </div>
    <div class="hero-visual">
      <div class="hero-mock-header"><span class="hero-mock-title">Voice of Customer</span><span class="hero-mock-badge">This Month</span></div>
      <div class="insight-item"><div class="insight-label">Top Pain Point</div><div class="insight-quote">"We're spending too much time on manual data entry after calls"</div><div class="insight-count">Mentioned in 47 calls</div></div>
      <div class="insight-item"><div class="insight-label">Competitor Mention</div><div class="insight-quote">"We looked at Gong but it's too expensive for our team size"</div><div class="insight-count">Mentioned in 23 calls</div></div>
      <div class="insight-item"><div class="insight-label">Feature Request</div><div class="insight-quote">"If it could just update Salesforce automatically, that'd be huge"</div><div class="insight-count">Mentioned in 31 calls</div></div>
    </div>
  </section>
  <section class="pain-section">
    <div class="pain-header"><p class="pain-label">The Problem</p><h2 class="pain-headline">Marketing in the dark</h2></div>
    <div class="pain-grid">
      <div class="pain-card"><div class="pain-stat">87%</div><p class="pain-desc">Of marketers never hear actual prospect conversations</p></div>
      <div class="pain-card"><div class="pain-stat">3x</div><p class="pain-desc">Longer to validate messaging without direct customer insight</p></div>
      <div class="pain-card"><div class="pain-stat">62%</div><p class="pain-desc">Of content misses the mark on actual buyer pain points</p></div>
    </div>
  </section>
  <section class="solution-section">
    <div class="solution-header"><p class="solution-label">How recrd helps</p><h2 class="solution-headline">Customer insights on tap</h2><p class="solution-sub">Real words from real prospects, automatically surfaced.</p></div>
    <div class="solution-grid">
      <div class="solution-card">
        <div class="solution-icon"><img src="/icons/feedback-loop.svg" alt="Pain Point Tracking" /></div>
        <h3 class="solution-title">Pain Point Tracking</h3>
        <p class="solution-desc">See which problems prospects mention most. Build messaging that speaks directly to real pain, not assumed pain.</p>
      </div>
      <div class="solution-card">
        <div class="solution-icon"><img src="/icons/battle-arena.svg" alt="Competitive Intel" /></div>
        <h3 class="solution-title">Competitive Intel</h3>
        <p class="solution-desc">Every competitor mention flagged and categorized. Know exactly how you're being compared and where you win.</p>
      </div>
      <div class="solution-card">
        <div class="solution-icon"><img src="/icons/megaphone.svg" alt="Content Fuel" /></div>
        <h3 class="solution-title">Content Fuel</h3>
        <p class="solution-desc">Surface exact phrases and objections for case studies, blog posts, and ad copy. Write content that converts.</p>
      </div>
      <div class="solution-card">
        <div class="solution-icon"><img src="/icons/signal-waves-purple.svg" alt="Lead Quality Insights" /></div>
        <h3 class="solution-title">Lead Quality Insights</h3>
        <p class="solution-desc">Understand which channels and campaigns produce conversations that convert, not just MQLs that ghost.</p>
      </div>
      <div class="solution-card solution-card-soon">
        <div class="solution-badge">Coming Soon</div>
        <div class="solution-icon"><img src="/icons/megaphone.svg" alt="Testimonial Agent" /></div>
        <h3 class="solution-title">Testimonial Agent</h3>
        <p class="solution-desc">Automatically flags quotable customer praise from calls. Build your case study pipeline without hunting for sound bites.</p>
      </div>
      <div class="solution-card solution-card-soon">
        <div class="solution-badge">Coming Soon</div>
        <div class="solution-icon"><img src="/icons/feedback-loop.svg" alt="Feature Request Agent" /></div>
        <h3 class="solution-title">Feature Request Agent</h3>
        <p class="solution-desc">Logs every feature request with context and frequency. Routes insights to product so nothing falls through the cracks.</p>
      </div>
    </div>
  </section>
  <section class="quote-section">
    <div class="quote-container"><div class="quote-mark">"</div><p class="quote-text">I used to schedule 'voice of customer' sessions with sales once a quarter. Now I have a live feed of exactly what prospects are saying. Game changer.</p><p class="quote-author"><strong>Rachel K.</strong> — Director of Product Marketing</p></div>
  </section>
  <section class="cta-section">
    <h2 class="cta-headline">Ready to hear your customers?</h2>
    <p class="cta-sub">Get real prospect insights delivered to your inbox, automatically.</p>
    <a href="#" class="btn">Book a Demo <span class="btn-arrow"><img src="/icons/chevron-green.svg" alt="" class="chevron-default" /><img src="/icons/chevron-white.svg" alt="" class="chevron-hover" /></span></a>
  </section>
  <Footer />
  <ThemeToggle />
</body>
</html>
