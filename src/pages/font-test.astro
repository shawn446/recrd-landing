---
// src/pages/font-test.astro
// Full landing page with font & theme switchers
---
<html lang="en" data-theme="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>recrd — Font & Theme Testing</title>
  <style>
    /* ============================================
       FONT FACE DECLARATIONS
       ============================================ */
    
    /* OBJECT SANS (Pangram Pangram) - Headlines */
    @font-face {
      font-family: 'Object Sans';
      src: url('/fonts/PPObjectSans-Heavy.otf') format('opentype');
      font-weight: 900;
      font-style: normal;
      font-display: swap;
    }
    @font-face {
      font-family: 'Object Sans';
      src: url('/fonts/PPObjectSans-Regular.otf') format('opentype');
      font-weight: 400;
      font-style: normal;
      font-display: swap;
    }

    /* FRAKTION MONO (Pangram Pangram) - Code/Data */
    @font-face {
      font-family: 'Fraktion Mono';
      src: url('/fonts/PPFraktionMono-Regular.otf') format('opentype');
      font-weight: 400;
      font-style: normal;
      font-display: swap;
    }

    /* ROOBERT (Displaay) - Variable Font */
    @font-face {
      font-family: 'Roobert';
      src: url('/fonts/RoobertCollectionVF-TRIAL.woff2') format('woff2');
      font-weight: 100 900;
      font-style: normal;
      font-display: swap;
    }

    /* MATTER (Displaay) - Static Fonts */
    @font-face {
      font-family: 'Matter';
      src: url('/fonts/Matter-TRIAL-Light.otf') format('opentype');
      font-weight: 300;
      font-style: normal;
      font-display: swap;
    }
    @font-face {
      font-family: 'Matter';
      src: url('/fonts/Matter-TRIAL-Regular.otf') format('opentype');
      font-weight: 400;
      font-style: normal;
      font-display: swap;
    }
    @font-face {
      font-family: 'Matter';
      src: url('/fonts/Matter-TRIAL-Medium.ttf') format('truetype');
      font-weight: 500;
      font-style: normal;
      font-display: swap;
    }

    /* ============================================
       CSS VARIABLES - THEME TOKENS
       ============================================ */
    :root {
      --mint: #46FCAF;
      --purple: #A678F4;
      --cyan: #02C0FF;
      --coral: #FF4F5E;
      --yellow: #FFE869;
      
      /* Body font - switchable */
      --body-font: 'Roobert', system-ui, sans-serif;
      --body-weight: 300;
    }

    /* DARK THEME (default) */
    [data-theme="dark"] {
      --bg: #0A0A0A;
      --bg-elevated: #111111;
      --bg-card: #161616;
      --border: #1a1a1a;
      --border-hover: #2a2a2a;
      --text-primary: #ffffff;
      --text-secondary: #a1a1aa;
      --text-muted: #52525b;
      --accent: var(--mint);
      --accent-text: #0a0a0a;
    }

    /* LIGHT THEME */
    [data-theme="light"] {
      --bg: #ffffff;
      --bg-elevated: #f8f8f8;
      --bg-card: #ffffff;
      --border: #e4e4e7;
      --border-hover: #d4d4d8;
      --text-primary: #09090b;
      --text-secondary: #52525b;
      --text-muted: #a1a1aa;
      --accent: #09090b;
      --accent-text: var(--mint);
    }

    /* ============================================
       RESET & BASE
       ============================================ */
    * { margin: 0; padding: 0; box-sizing: border-box; }
    
    html {
      scroll-behavior: smooth;
    }

    body {
      background: var(--bg);
      color: var(--text-primary);
      font-family: var(--body-font);
      font-weight: var(--body-weight);
      line-height: 1.6;
      transition: background 0.3s ease, color 0.3s ease;
    }

    /* ============================================
       FLOATING CONTROLS
       ============================================ */
    .controls {
      position: fixed;
      top: 20px;
      right: 20px;
      z-index: 1000;
      display: flex;
      gap: 8px;
      background: var(--bg-elevated);
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 8px;
    }

    .control-group {
      display: flex;
      gap: 4px;
      align-items: center;
    }

    .control-group + .control-group {
      border-left: 1px solid var(--border);
      padding-left: 8px;
    }

    .control-btn {
      background: transparent;
      border: 1px solid transparent;
      color: var(--text-muted);
      padding: 8px 12px;
      font-size: 12px;
      font-family: 'Fraktion Mono', monospace;
      cursor: pointer;
      border-radius: 6px;
      transition: all 0.2s;
    }

    .control-btn:hover {
      color: var(--text-secondary);
      background: var(--bg-card);
    }

    .control-btn.active {
      background: var(--accent);
      color: var(--accent-text);
      border-color: var(--accent);
    }

    .control-label {
      font-size: 9px;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 1px;
      padding: 0 8px;
    }

    /* ============================================
       NAV
       ============================================ */
    .nav {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 20px 40px;
      border-bottom: 1px solid var(--border);
    }

    .nav-logo {
      font-family: 'Object Sans', system-ui, sans-serif;
      font-weight: 900;
      font-size: 24px;
      letter-spacing: -0.02em;
      color: var(--text-primary);
      text-decoration: none;
    }

    .nav-links {
      display: flex;
      gap: 32px;
      list-style: none;
    }

    .nav-links a {
      color: var(--text-secondary);
      text-decoration: none;
      font-size: 14px;
      font-weight: 400;
      transition: color 0.2s;
    }

    .nav-links a:hover {
      color: var(--text-primary);
    }

    .nav-cta {
      background: var(--accent);
      color: var(--accent-text);
      padding: 10px 20px;
      font-family: 'Object Sans', system-ui, sans-serif;
      font-weight: 400;
      font-size: 14px;
      text-decoration: none;
      transition: opacity 0.2s;
    }

    .nav-cta:hover {
      opacity: 0.9;
    }

    /* ============================================
       HERO
       ============================================ */
    .hero {
      padding: 120px 40px 80px;
      text-align: center;
      position: relative;
      overflow: hidden;
    }

    .hero::before {
      content: '';
      position: absolute;
      top: -300px;
      left: 50%;
      transform: translateX(-50%);
      width: 800px;
      height: 800px;
      background: radial-gradient(circle, rgba(70, 252, 175, 0.08) 0%, transparent 60%);
      pointer-events: none;
    }

    .hero-badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: var(--bg-elevated);
      border: 1px solid var(--border);
      padding: 8px 16px;
      border-radius: 100px;
      font-size: 12px;
      color: var(--text-secondary);
      margin-bottom: 32px;
    }

    .hero-badge-dot {
      width: 6px;
      height: 6px;
      background: var(--mint);
      border-radius: 50%;
      animation: pulse 2s infinite;
    }

    @keyframes pulse {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.5; }
    }

    .hero-headline {
      font-family: 'Object Sans', system-ui, sans-serif;
      font-weight: 900;
      font-size: clamp(48px, 8vw, 72px);
      letter-spacing: -0.03em;
      line-height: 1.0;
      margin-bottom: 24px;
      max-width: 900px;
      margin-left: auto;
      margin-right: auto;
    }

    .hero-headline em {
      font-style: normal;
      color: var(--mint);
    }

    .hero-sub {
      font-size: 20px;
      font-weight: var(--body-weight);
      color: var(--text-secondary);
      max-width: 580px;
      margin: 0 auto 40px;
      line-height: 1.6;
    }

    .hero-capture {
      display: flex;
      gap: 0;
      max-width: 440px;
      margin: 0 auto;
    }

    .hero-capture input {
      flex: 1;
      padding: 16px 20px;
      background: var(--bg-elevated);
      border: 1px solid var(--border);
      border-right: none;
      color: var(--text-primary);
      font-size: 14px;
      font-family: var(--body-font);
      outline: none;
    }

    .hero-capture input::placeholder {
      color: var(--text-muted);
    }

    .hero-capture button {
      padding: 16px 28px;
      background: var(--accent);
      color: var(--accent-text);
      border: none;
      font-family: 'Object Sans', system-ui, sans-serif;
      font-weight: 400;
      font-size: 14px;
      cursor: pointer;
      transition: opacity 0.2s;
    }

    .hero-capture button:hover {
      opacity: 0.9;
    }

    /* ============================================
       AGENT CARDS (Hero Visual)
       ============================================ */
    .agent-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 16px;
      max-width: 900px;
      margin: 80px auto 0;
    }

    .agent-card {
      background: var(--bg-card);
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 24px;
      text-align: left;
      transition: border-color 0.2s, transform 0.2s;
    }

    .agent-card:hover {
      border-color: var(--border-hover);
      transform: translateY(-2px);
    }

    .agent-card-header {
      display: flex;
      align-items: center;
      gap: 12px;
      margin-bottom: 16px;
    }

    .agent-icon {
      width: 36px;
      height: 36px;
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .agent-icon svg {
      width: 18px;
      height: 18px;
    }

    .agent-icon.pink { background: rgba(255, 79, 94, 0.15); color: var(--coral); }
    .agent-icon.teal { background: rgba(70, 252, 175, 0.15); color: var(--mint); }
    .agent-icon.purple { background: rgba(166, 120, 244, 0.15); color: var(--purple); }
    .agent-icon.yellow { background: rgba(255, 232, 105, 0.15); color: var(--yellow); }
    .agent-icon.blue { background: rgba(2, 192, 255, 0.15); color: var(--cyan); }

    .agent-status {
      font-family: 'Fraktion Mono', monospace;
      font-size: 10px;
      color: var(--mint);
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .agent-name {
      font-family: 'Object Sans', system-ui, sans-serif;
      font-weight: 400;
      font-size: 14px;
      color: var(--text-primary);
    }

    .agent-output {
      font-size: 13px;
      color: var(--text-secondary);
      font-weight: var(--body-weight);
    }

    .agent-output p {
      margin-top: 8px;
      font-style: italic;
      opacity: 0.8;
    }

    .agent-metric {
      display: flex;
      align-items: baseline;
      gap: 4px;
    }

    .metric-value {
      font-family: 'Fraktion Mono', monospace;
      font-size: 24px;
      color: var(--mint);
    }

    .metric-label {
      font-size: 12px;
      color: var(--text-muted);
    }

    /* ============================================
       FEATURES SECTION
       ============================================ */
    .features {
      padding: 120px 40px;
      border-top: 1px solid var(--border);
    }

    .features-header {
      text-align: center;
      margin-bottom: 80px;
    }

    .features-label {
      font-family: 'Fraktion Mono', monospace;
      font-size: 11px;
      color: var(--purple);
      text-transform: uppercase;
      letter-spacing: 1.5px;
      margin-bottom: 16px;
    }

    .features-title {
      font-family: 'Object Sans', system-ui, sans-serif;
      font-weight: 900;
      font-size: 40px;
      letter-spacing: -0.02em;
      max-width: 600px;
      margin: 0 auto;
    }

    .features-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 24px;
      max-width: 1100px;
      margin: 0 auto;
    }

    .feature-card {
      background: var(--bg-card);
      border: 1px solid var(--border);
      border-radius: 16px;
      padding: 32px;
      transition: border-color 0.2s;
    }

    .feature-card:hover {
      border-color: var(--border-hover);
    }

    .feature-icon {
      width: 48px;
      height: 48px;
      background: var(--bg-elevated);
      border: 1px solid var(--border);
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 20px;
      color: var(--mint);
    }

    .feature-icon svg {
      width: 24px;
      height: 24px;
    }

    .feature-title {
      font-family: 'Object Sans', system-ui, sans-serif;
      font-weight: 400;
      font-size: 18px;
      margin-bottom: 8px;
    }

    .feature-desc {
      font-size: 14px;
      font-weight: var(--body-weight);
      color: var(--text-secondary);
      line-height: 1.6;
    }

    /* ============================================
       PERSONAS SECTION
       ============================================ */
    .personas {
      padding: 120px 40px;
      border-top: 1px solid var(--border);
    }

    .personas-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-end;
      max-width: 1100px;
      margin: 0 auto 60px;
    }

    .personas-label {
      font-family: 'Fraktion Mono', monospace;
      font-size: 11px;
      color: var(--cyan);
      text-transform: uppercase;
      letter-spacing: 1.5px;
      margin-bottom: 16px;
    }

    .personas-title {
      font-family: 'Object Sans', system-ui, sans-serif;
      font-weight: 900;
      font-size: 40px;
      letter-spacing: -0.02em;
    }

    .personas-title em {
      font-style: normal;
      color: var(--mint);
    }

    .personas-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 24px;
      max-width: 1100px;
      margin: 0 auto;
    }

    .persona-card {
      background: var(--bg-card);
      border: 1px solid var(--border);
      border-radius: 16px;
      padding: 32px;
    }

    .persona-title {
      font-family: 'Object Sans', system-ui, sans-serif;
      font-weight: 400;
      font-size: 20px;
      margin-bottom: 12px;
    }

    .persona-desc {
      font-size: 14px;
      font-weight: var(--body-weight);
      color: var(--text-secondary);
      line-height: 1.6;
      margin-bottom: 20px;
    }

    .persona-pills {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
    }

    .persona-pill {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      background: var(--bg-elevated);
      border: 1px solid var(--border);
      padding: 6px 12px;
      border-radius: 100px;
      font-size: 12px;
      color: var(--text-secondary);
    }

    .persona-pill svg {
      width: 14px;
      height: 14px;
      color: var(--mint);
    }

    /* ============================================
       CTA SECTION
       ============================================ */
    .cta {
      padding: 120px 40px;
      border-top: 1px solid var(--border);
    }

    .cta-card {
      max-width: 800px;
      margin: 0 auto;
      background: var(--bg-card);
      border: 1px solid var(--border);
      border-radius: 24px;
      padding: 80px 60px;
      text-align: center;
      position: relative;
      overflow: hidden;
    }

    .cta-card::before {
      content: '';
      position: absolute;
      top: -100px;
      right: -100px;
      width: 400px;
      height: 400px;
      background: radial-gradient(circle, rgba(166, 120, 244, 0.1) 0%, transparent 60%);
      pointer-events: none;
    }

    .cta-title {
      font-family: 'Object Sans', system-ui, sans-serif;
      font-weight: 900;
      font-size: 36px;
      letter-spacing: -0.02em;
      margin-bottom: 16px;
      position: relative;
    }

    .cta-sub {
      font-size: 16px;
      font-weight: var(--body-weight);
      color: var(--text-secondary);
      margin-bottom: 32px;
      position: relative;
    }

    .cta-btn {
      display: inline-block;
      background: var(--accent);
      color: var(--accent-text);
      padding: 16px 32px;
      font-family: 'Object Sans', system-ui, sans-serif;
      font-weight: 400;
      font-size: 14px;
      text-decoration: none;
      transition: opacity 0.2s;
      position: relative;
    }

    .cta-btn:hover {
      opacity: 0.9;
    }

    /* ============================================
       FOOTER
       ============================================ */
    .footer {
      padding: 40px;
      border-top: 1px solid var(--border);
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .footer-logo {
      font-family: 'Object Sans', system-ui, sans-serif;
      font-weight: 900;
      font-size: 20px;
      color: var(--text-primary);
    }

    .footer-links {
      display: flex;
      gap: 24px;
      list-style: none;
    }

    .footer-links a {
      color: var(--text-muted);
      text-decoration: none;
      font-size: 13px;
      transition: color 0.2s;
    }

    .footer-links a:hover {
      color: var(--text-secondary);
    }

    .footer-copy {
      font-size: 13px;
      color: var(--text-muted);
    }

    /* ============================================
       RESPONSIVE
       ============================================ */
    @media (max-width: 900px) {
      .agent-grid,
      .features-grid,
      .personas-grid {
        grid-template-columns: 1fr;
      }

      .nav-links {
        display: none;
      }

      .personas-header {
        flex-direction: column;
        align-items: flex-start;
        gap: 20px;
      }

      .controls {
        top: auto;
        bottom: 20px;
        right: 50%;
        transform: translateX(50%);
      }
    }
  </style>
</head>
<body>

  <!-- FLOATING CONTROLS -->
  <div class="controls">
    <div class="control-group">
      <span class="control-label">Font</span>
      <button class="control-btn active" id="btn-roobert" onclick="switchFont('roobert')">Roobert</button>
      <button class="control-btn" id="btn-matter" onclick="switchFont('matter')">Matter</button>
    </div>
    <div class="control-group">
      <span class="control-label">Weight</span>
      <button class="control-btn" id="btn-100" onclick="switchWeight(100)">100</button>
      <button class="control-btn" id="btn-200" onclick="switchWeight(200)">200</button>
      <button class="control-btn active" id="btn-300" onclick="switchWeight(300)">300</button>
      <button class="control-btn" id="btn-400" onclick="switchWeight(400)">400</button>
    </div>
    <div class="control-group">
      <span class="control-label">Theme</span>
      <button class="control-btn active" id="btn-dark" onclick="switchTheme('dark')">Dark</button>
      <button class="control-btn" id="btn-light" onclick="switchTheme('light')">Light</button>
    </div>
  </div>

  <!-- NAV -->
  <nav class="nav">
    <a href="/" class="nav-logo">recrd</a>
    <ul class="nav-links">
      <li><a href="#">Product</a></li>
      <li><a href="#">Pricing</a></li>
      <li><a href="#">Docs</a></li>
      <li><a href="#">Blog</a></li>
    </ul>
    <a href="#" class="nav-cta">Get Started</a>
  </nav>

  <!-- HERO -->
  <section class="hero">
    <div class="hero-badge">
      <span class="hero-badge-dot"></span>
      <span>Now in Beta</span>
    </div>
    
    <h1 class="hero-headline">Every conversation triggers an army of <em>AI agents</em></h1>
    
    <p class="hero-sub">One call. Six agents. Zero setup. They draft follow-ups, update your CRM, score deals, flag risks, and coach your team — automatically.</p>
    
    <div class="hero-capture">
      <input type="email" placeholder="What's your work email?">
      <button>Get Started</button>
    </div>

    <!-- Agent Cards -->
    <div class="agent-grid">
      <div class="agent-card">
        <div class="agent-card-header">
          <div class="agent-icon pink">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg>
          </div>
          <span class="agent-status">Live</span>
        </div>
        <div class="agent-name">Follow-up Agent</div>
        <div class="agent-output">
          Draft ready
          <p>"Hi Sarah, following up on our call about the Q1 rollout..."</p>
        </div>
      </div>

      <div class="agent-card">
        <div class="agent-card-header">
          <div class="agent-icon teal">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><ellipse cx="12" cy="5" rx="9" ry="3"/><path d="M21 12c0 1.66-4 3-9 3s-9-1.34-9-3"/><path d="M3 5v14c0 1.66 4 3 9 3s9-1.34 9-3V5"/></svg>
          </div>
          <span class="agent-status">Live</span>
        </div>
        <div class="agent-name">CRM Agent</div>
        <div class="agent-output">
          <div class="agent-metric">
            <span class="metric-value">3</span>
            <span class="metric-label">fields updated</span>
          </div>
        </div>
      </div>

      <div class="agent-card">
        <div class="agent-card-header">
          <div class="agent-icon purple">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87M16 3.13a4 4 0 0 1 0 7.75"/></svg>
          </div>
          <span class="agent-status">Live</span>
        </div>
        <div class="agent-name">Coaching Agent</div>
        <div class="agent-output">
          <div class="agent-metric">
            <span class="metric-value">8.4</span>
            <span class="metric-label">/ 10 score</span>
          </div>
        </div>
      </div>

      <div class="agent-card">
        <div class="agent-card-header">
          <div class="agent-icon yellow">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 3v18h18"/><path d="m19 9-5 5-4-4-3 3"/></svg>
          </div>
          <span class="agent-status">Live</span>
        </div>
        <div class="agent-name">Forecast Agent</div>
        <div class="agent-output">
          <div class="agent-metric">
            <span class="metric-value">78%</span>
            <span class="metric-label">win probability</span>
          </div>
        </div>
      </div>

      <div class="agent-card">
        <div class="agent-card-header">
          <div class="agent-icon blue">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/><rect x="2" y="9" width="4" height="12"/><circle cx="4" cy="4" r="2"/></svg>
          </div>
          <span class="agent-status">Live</span>
        </div>
        <div class="agent-name">Content Agent</div>
        <div class="agent-output">
          LinkedIn draft ready
          <p>"Just wrapped a great call about revenue ops..."</p>
        </div>
      </div>

      <div class="agent-card">
        <div class="agent-card-header">
          <div class="agent-icon pink">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg>
          </div>
          <span class="agent-status">Live</span>
        </div>
        <div class="agent-name">Risk Agent</div>
        <div class="agent-output">
          Low risk detected
          <p>Champion engaged, budget confirmed</p>
        </div>
      </div>
    </div>
  </section>

  <!-- FEATURES -->
  <section class="features">
    <div class="features-header">
      <p class="features-label">For Sales Teams</p>
      <h2 class="features-title">AI agents that actually understand your deals</h2>
    </div>

    <div class="features-grid">
      <div class="feature-card">
        <div class="feature-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/></svg>
        </div>
        <h3 class="feature-title">Deal Intelligence</h3>
        <p class="feature-desc">Extract key moments, objections, and buying signals from every call. Know exactly where each deal stands.</p>
      </div>

      <div class="feature-card">
        <div class="feature-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 3v18h18"/><path d="m19 9-5 5-4-4-3 3"/></svg>
        </div>
        <h3 class="feature-title">Pipeline Forecasting</h3>
        <p class="feature-desc">See which deals are real and which are wishful thinking — based on actual conversation signals.</p>
      </div>

      <div class="feature-card">
        <div class="feature-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87M16 3.13a4 4 0 0 1 0 7.75"/></svg>
        </div>
        <h3 class="feature-title">1:1 Prep</h3>
        <p class="feature-desc">Get AI-prepped briefings that surface what actually matters — risks, coaching moments, and wins.</p>
      </div>
    </div>
  </section>

  <!-- PERSONAS -->
  <section class="personas">
    <div class="personas-header">
      <div>
        <p class="personas-label">Built for your whole team</p>
        <h2 class="personas-title">AI agents that know <em>exactly</em> what you need</h2>
      </div>
    </div>

    <div class="personas-grid">
      <div class="persona-card">
        <h3 class="persona-title">For Sales Reps</h3>
        <p class="persona-desc">Stop taking notes. Start closing deals. Every call analyzed, every follow-up prepped, every CRM field filled.</p>
        <div class="persona-pills">
          <span class="persona-pill">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 1a3 3 0 0 0-3 3v8a3 3 0 0 0 6 0V4a3 3 0 0 0-3-3z"/><path d="M19 10v2a7 7 0 0 1-14 0v-2"/></svg>
            Auto Notes
          </span>
          <span class="persona-pill">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg>
            Email Drafts
          </span>
          <span class="persona-pill">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/></svg>
            CRM Sync
          </span>
        </div>
      </div>

      <div class="persona-card">
        <h3 class="persona-title">For Sales Managers</h3>
        <p class="persona-desc">See every deal without watching every call. Coaching moments surfaced, risks flagged, forecasts you can trust.</p>
        <div class="persona-pills">
          <span class="persona-pill">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 3v18h18"/><path d="m19 9-5 5-4-4-3 3"/></svg>
            Forecasting
          </span>
          <span class="persona-pill">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/></svg>
            1:1 Prep
          </span>
          <span class="persona-pill">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4"/><path d="M12 8h.01"/></svg>
            Risk Alerts
          </span>
        </div>
      </div>

      <div class="persona-card">
        <h3 class="persona-title">For RevOps</h3>
        <p class="persona-desc">Clean CRM data without chasing reps. Every field filled, every stage updated, zero manual work.</p>
        <div class="persona-pills">
          <span class="persona-pill">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><ellipse cx="12" cy="5" rx="9" ry="3"/><path d="M21 12c0 1.66-4 3-9 3s-9-1.34-9-3"/><path d="M3 5v14c0 1.66 4 3 9 3s9-1.34 9-3V5"/></svg>
            Data Hygiene
          </span>
          <span class="persona-pill">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>
            Pipeline
          </span>
          <span class="persona-pill">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><line x1="3" y1="9" x2="21" y2="9"/><line x1="9" y1="21" x2="9" y2="9"/></svg>
            Reporting
          </span>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="cta">
    <div class="cta-card">
      <h2 class="cta-title">Start closing more deals with AI agents that actually work</h2>
      <p class="cta-sub">No credit card required. Free for teams up to 5 users.</p>
      <a href="#" class="cta-btn">Get Started For Free</a>
    </div>
  </section>

  <!-- FOOTER -->
  <footer class="footer">
    <div class="footer-logo">recrd</div>
    <ul class="footer-links">
      <li><a href="#">Privacy</a></li>
      <li><a href="#">Terms</a></li>
      <li><a href="#">Contact</a></li>
    </ul>
    <span class="footer-copy">© 2025 recrd</span>
  </footer>

  <script>
    // Font switcher
    function switchFont(font) {
      const root = document.documentElement;
      
      if (font === 'roobert') {
        root.style.setProperty('--body-font', "'Roobert', system-ui, sans-serif");
      } else if (font === 'matter') {
        root.style.setProperty('--body-font', "'Matter', system-ui, sans-serif");
      }

      // Update button states
      document.getElementById('btn-roobert').classList.toggle('active', font === 'roobert');
      document.getElementById('btn-matter').classList.toggle('active', font === 'matter');
    }

    // Weight switcher
    function switchWeight(weight) {
      const root = document.documentElement;
      root.style.setProperty('--body-weight', weight);

      // Update button states
      [100, 200, 300, 400].forEach(w => {
        document.getElementById('btn-' + w).classList.toggle('active', w === weight);
      });
    }

    // Theme switcher
    function switchTheme(theme) {
      document.documentElement.setAttribute('data-theme', theme);

      // Update button states
      document.getElementById('btn-dark').classList.toggle('active', theme === 'dark');
      document.getElementById('btn-light').classList.toggle('active', theme === 'light');
    }
  </script>

</body>
</html>
