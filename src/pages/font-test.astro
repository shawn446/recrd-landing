---
// Hero Section - Simplified with Solid Color Cards
---

<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>recrd - AI agents that close deals</title>
  
  <style>
    @font-face {
      font-family: 'Roobert';
      src: url('/fonts/RoobertCollectionVF-TRIAL.woff2') format('woff2');
      font-weight: 100 900;
      font-style: normal;
      font-display: swap;
    }
    @font-face {
      font-family: 'Fraktion Mono';
      src: url('/fonts/PPFraktionMono-Regular.otf') format('opentype');
      font-weight: 400;
      font-style: normal;
      font-display: swap;
    }

    :root {
      --font: 'Roobert', -apple-system, BlinkMacSystemFont, sans-serif;
      --mono: 'Fraktion Mono', 'SF Mono', monospace;
      --mint: #46FCAF;
      --purple: #A678F4;
      --cyan: #02C0FF;
      --coral: #FF4F5E;
    }

    [data-theme="dark"] {
      --bg: #0A0A0A;
      --text: #ffffff;
      --text-muted: #888888;
      --border: rgba(255,255,255,0.12);
    }

    [data-theme="light"] {
      --bg: #fafafa;
      --text: #0a0a0a;
      --text-muted: #666666;
      --border: rgba(0,0,0,0.1);
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }
    html, body { overflow-x: hidden; }

    body {
      font-family: var(--font);
      background: var(--bg);
      color: var(--text);
      min-height: 100vh;
    }

    /* NAV */
    nav {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      height: 72px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0 48px;
      z-index: 100;
      background: rgba(10,10,10,0.85);
      backdrop-filter: blur(20px);
      border-bottom: 1px solid var(--border);
    }

    .nav-logo {
      display: flex;
      align-items: center;
      gap: 12px;
      text-decoration: none;
    }
    .nav-logo:hover { opacity: 0.8; }
    .nav-logo svg { width: 32px; height: 32px; }
    .nav-wordmark { font-weight: 600; font-size: 18px; color: var(--text); }

    .nav-links { display: flex; gap: 8px; list-style: none; }
    .nav-links a {
      color: var(--text-muted);
      text-decoration: none;
      font-size: 14px;
      font-weight: 500;
      padding: 10px 16px;
      transition: all 0.2s ease;
    }
    .nav-links li:nth-child(1) a:hover { background: var(--cyan); color: #0a0a0a; }
    .nav-links li:nth-child(2) a:hover { background: var(--mint); color: #0a0a0a; }
    .nav-links li:nth-child(3) a:hover { background: var(--purple); color: #fff; }
    .nav-links li:nth-child(4) a:hover { background: var(--coral); color: #fff; }

    .nav-cta {
      background: var(--text);
      color: var(--bg);
      padding: 12px 24px;
      font-weight: 500;
      font-size: 14px;
      text-decoration: none;
      transition: all 0.2s ease;
    }
    .nav-cta:hover { background: var(--mint); color: #0a0a0a; }

    /* HERO */
    .hero {
      min-height: 100vh;
      display: flex;
      align-items: center;
      padding: 72px 0 0 48px;
      position: relative;
      overflow: hidden;
    }

    /* Background Effects */
    .hero-bg {
      position: absolute;
      inset: 0;
      z-index: 0;
      overflow: hidden;
    }

    .glow-orb {
      position: absolute;
      border-radius: 50%;
      filter: blur(150px);
    }

    .glow-orb.mint {
      width: 600px;
      height: 600px;
      background: var(--mint);
      top: -15%;
      right: 20%;
      opacity: 0.12;
    }

    .glow-orb.purple {
      width: 500px;
      height: 500px;
      background: var(--purple);
      top: 40%;
      right: 40%;
      opacity: 0.1;
    }

    .glow-orb.coral {
      width: 400px;
      height: 400px;
      background: var(--coral);
      bottom: 10%;
      right: 15%;
      opacity: 0.08;
    }

    .hero-content {
      max-width: 520px;
      z-index: 10;
      padding: 48px 0;
    }

    .hero-headline {
      font-size: clamp(48px, 7vw, 80px);
      font-weight: 700;
      line-height: 0.95;
      letter-spacing: -0.035em;
      margin-bottom: 24px;
    }
    .hero-headline em { font-style: normal; color: var(--mint); }
    [data-theme="light"] .hero-headline em { color: var(--purple); }

    .hero-sub {
      font-size: clamp(22px, 3vw, 32px);
      font-weight: 500;
      line-height: 1.2;
      margin-bottom: 20px;
      letter-spacing: -0.02em;
    }

    .hero-body {
      font-size: 17px;
      font-weight: 300;
      color: var(--text-muted);
      line-height: 1.6;
      margin-bottom: 40px;
    }

    .hero-buttons { display: flex; gap: 16px; }

    .btn {
      padding: 16px 32px;
      font-weight: 500;
      font-size: 15px;
      text-decoration: none;
      transition: all 0.2s ease;
      cursor: pointer;
    }
    .btn-primary { background: #fff; color: #0a0a0a; }
    .btn-primary:hover { background: var(--mint); transform: translateY(-2px); }
    .btn-secondary { background: transparent; color: var(--text); border: 1px solid var(--border); }
    .btn-secondary:hover { background: var(--cyan); border-color: var(--cyan); color: #0a0a0a; }

    /* ========================================
       CARDS SYSTEM
       ======================================== */
    
    .cards-container {
      position: absolute;
      top: 72px;
      right: 0;
      bottom: 0;
      width: 55%;
      min-width: 600px;
      z-index: 5;
    }

    /* SVG Paths */
    .paths-svg {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: 1;
      pointer-events: none;
    }

    .flow-path {
      fill: none;
      stroke-width: 2;
      stroke-linecap: square;
      stroke-linejoin: miter;
      filter: drop-shadow(0 0 8px currentColor);
    }

    /* Solid Color Cards */
    .card {
      position: absolute;
      padding: 24px 28px;
      opacity: 0;
      transform: translateY(20px) scale(0.95);
      z-index: 10;
      width: 300px;
    }

    .card-header {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 14px;
    }

    .card-icon {
      width: 22px;
      height: 22px;
      flex-shrink: 0;
    }
    .card-icon svg {
      width: 100%;
      height: 100%;
      stroke-width: 2;
      stroke-linecap: round;
      stroke-linejoin: round;
      fill: none;
    }

    .card-badge {
      font-family: var(--mono);
      font-size: 9px;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      padding: 5px 10px;
    }

    .card-title {
      font-size: 20px;
      font-weight: 600;
      line-height: 1.3;
      margin-bottom: 8px;
    }

    .card-content {
      font-size: 14px;
      line-height: 1.5;
      opacity: 0.9;
    }

    .card-meta {
      font-family: var(--mono);
      font-size: 11px;
      margin-top: 14px;
      opacity: 0.7;
    }

    /* Card solid colors */
    .card.mint {
      background: var(--mint);
      color: #0a0a0a;
      box-shadow: 0 8px 40px rgba(70, 252, 175, 0.3);
    }
    .card.mint .card-badge { background: rgba(0,0,0,0.15); }
    .card.mint .card-icon svg { stroke: #0a0a0a; }

    .card.purple {
      background: var(--purple);
      color: #fff;
      box-shadow: 0 8px 40px rgba(166, 120, 244, 0.35);
    }
    .card.purple .card-badge { background: rgba(255,255,255,0.2); }
    .card.purple .card-icon svg { stroke: #fff; }

    .card.cyan {
      background: var(--cyan);
      color: #0a0a0a;
      box-shadow: 0 8px 40px rgba(2, 192, 255, 0.3);
    }
    .card.cyan .card-badge { background: rgba(0,0,0,0.15); }
    .card.cyan .card-icon svg { stroke: #0a0a0a; }

    .card.coral {
      background: var(--coral);
      color: #fff;
      box-shadow: 0 8px 40px rgba(255, 79, 94, 0.35);
    }
    .card.coral .card-badge { background: rgba(255,255,255,0.2); }
    .card.coral .card-icon svg { stroke: #fff; }

    /* Stats grid */
    .stats-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 8px;
      margin-top: 12px;
    }

    .stat-item {
      background: rgba(255,255,255,0.15);
      padding: 10px 12px;
    }

    .stat-label {
      font-family: var(--mono);
      font-size: 9px;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      opacity: 0.8;
      margin-bottom: 4px;
    }

    .stat-value {
      font-size: 14px;
      font-weight: 600;
    }

    /* Quote */
    .quote-text {
      font-style: italic;
      padding: 12px;
      background: rgba(255,255,255,0.15);
      margin-bottom: 8px;
      font-size: 13px;
      line-height: 1.4;
    }

    /* MEDDIC */
    .meddic-bar {
      display: flex;
      gap: 4px;
      margin-top: 10px;
    }
    .meddic-dot {
      flex: 1;
      height: 8px;
      background: rgba(0,0,0,0.2);
    }
    .meddic-dot.filled {
      background: rgba(0,0,0,0.5);
    }

    /* Card positions */
    #card1 { top: 8%; left: 8%; }
    #card2 { top: 28%; left: 45%; }
    #card3 { top: 52%; left: 55%; }
    #card4 { top: 72%; left: 15%; }

    /* Theme toggle */
    .theme-toggle {
      position: fixed;
      bottom: 24px;
      left: 50%;
      transform: translateX(-50%);
      display: flex;
      background: var(--bg);
      border: 1px solid var(--border);
      z-index: 1000;
    }
    .theme-toggle button {
      padding: 10px 20px;
      background: none;
      border: none;
      color: var(--text-muted);
      font-size: 13px;
      cursor: pointer;
    }
    .theme-toggle button:hover:not(.active) { color: var(--text); }
    .theme-toggle button.active { background: var(--text); color: var(--bg); }

    /* RESPONSIVE */
    @media (max-width: 1200px) {
      .cards-container { width: 50%; min-width: 500px; }
      .card { width: 260px; padding: 20px 24px; }
      .card-title { font-size: 18px; }
    }

    @media (max-width: 1000px) {
      .cards-container { width: 45%; min-width: 400px; }
      .card { width: 220px; padding: 16px 20px; }
      .card-title { font-size: 16px; }
      .card-content { font-size: 12px; }
      .stats-grid { display: none; }
    }

    @media (max-width: 900px) {
      .hero {
        flex-direction: column;
        padding: 100px 24px 48px;
        min-height: auto;
      }
      .hero-content { max-width: 100%; text-align: center; margin-bottom: 40px; }
      .hero-buttons { justify-content: center; }
      .cards-container {
        position: relative;
        width: 100%;
        min-width: auto;
        height: 500px;
      }
      .card { width: 200px; padding: 14px 16px; }
      .card-title { font-size: 14px; }
      .card-content { display: none; }
      .card-meta { display: none; }
      .nav-links { display: none; }
      nav { padding: 0 24px; }
    }

    @media (max-width: 600px) {
      .hero-headline { font-size: 36px; }
      .hero-sub { font-size: 20px; }
      .hero-buttons { flex-direction: column; }
      .btn { width: 100%; text-align: center; }
      .cards-container { height: 400px; }
      .card { width: 160px; padding: 12px; }
      .card-badge { font-size: 7px; padding: 3px 6px; }
      .card-title { font-size: 12px; }
      .card-icon { width: 16px; height: 16px; }
    }
  </style>
</head>

<body>
  <nav>
    <a href="/" class="nav-logo">
      <svg viewBox="0 0 52 52" fill="none">
        <rect y="16" width="28" height="28" fill="#A678F4"/>
        <rect x="16" width="28" height="28" rx="6" fill="#46FCAF"/>
      </svg>
      <span class="nav-wordmark">recrd</span>
    </a>
    <ul class="nav-links">
      <li><a href="#">Features</a></li>
      <li><a href="#">Pricing</a></li>
      <li><a href="#">Docs</a></li>
      <li><a href="#">Blog</a></li>
    </ul>
    <a href="#" class="nav-cta">Dashboard</a>
  </nav>

  <section class="hero">
    <div class="hero-bg">
      <div class="glow-orb mint"></div>
      <div class="glow-orb purple"></div>
      <div class="glow-orb coral"></div>
    </div>

    <div class="hero-content">
      <h1 class="hero-headline">AI agents that <em>close deals</em></h1>
      <p class="hero-sub">One call. Six agents. Zero setup.</p>
      <p class="hero-body">Record your sales calls and let AI handle the follow-ups, CRM updates, and coaching insights automatically.</p>
      <div class="hero-buttons">
        <a href="#" class="btn btn-primary">Get Started Free</a>
        <a href="#" class="btn btn-secondary">Watch Demo</a>
      </div>
    </div>

    <div class="cards-container">
      <!-- SVG Paths -->
      <svg class="paths-svg" viewBox="0 0 100 100" preserveAspectRatio="none">
        <!-- Path 1: Card1 → Card2 -->
        <path id="path1" class="flow-path" stroke="#46FCAF" vector-effect="non-scaling-stroke"
              d="M 25,18 L 25,32 L 45,32"/>
        
        <!-- Path 2: Card2 → Card3 -->
        <path id="path2" class="flow-path" stroke="#A678F4" vector-effect="non-scaling-stroke"
              d="M 65,42 L 65,55"/>
        
        <!-- Path 3: Card3 → Card4 -->
        <path id="path3" class="flow-path" stroke="#02C0FF" vector-effect="non-scaling-stroke"
              d="M 55,65 L 55,76 L 35,76"/>
      </svg>

      <!-- Card 1: Call Completed -->
      <div class="card mint" id="card1">
        <div class="card-header">
          <div class="card-icon">
            <svg viewBox="0 0 24 24">
              <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
            </svg>
          </div>
          <span class="card-badge">Call Ended</span>
        </div>
        <span class="card-title">Call completed</span>
        <div class="card-content">Discovery call with Acme Corp</div>
        <div class="card-meta">32 min · Zoom · Sarah Chen</div>
      </div>

      <!-- Card 2: recrd Analyzes -->
      <div class="card purple" id="card2">
        <div class="card-header">
          <div class="card-icon">
            <svg viewBox="0 0 24 24">
              <path d="M2 12h2"/><path d="M6 8v8"/><path d="M10 4v16"/><path d="M14 6v12"/><path d="M18 9v6"/><path d="M22 12h-2"/>
            </svg>
          </div>
          <span class="card-badge">Processing</span>
        </div>
        <span class="card-title">recrd analyzes</span>
        <div class="card-content">Extracting insights from transcript</div>
        <div class="stats-grid">
          <div class="stat-item">
            <div class="stat-label">Sentiment</div>
            <div class="stat-value">87% Positive</div>
          </div>
          <div class="stat-item">
            <div class="stat-label">Momentum</div>
            <div class="stat-value">High</div>
          </div>
        </div>
        <div class="card-meta">6 agents deployed</div>
      </div>

      <!-- Card 3: Pipeline Updated -->
      <div class="card cyan" id="card3">
        <div class="card-header">
          <div class="card-icon">
            <svg viewBox="0 0 24 24">
              <line x1="18" y1="20" x2="18" y2="10"/><line x1="12" y1="20" x2="12" y2="4"/><line x1="6" y1="20" x2="6" y2="14"/>
            </svg>
          </div>
          <span class="card-badge">CRM Agent</span>
        </div>
        <span class="card-title">Pipeline updated</span>
        <div class="card-content">Stage → Negotiation<br>5/7 MEDDIC fields synced</div>
        <div class="meddic-bar">
          <div class="meddic-dot filled"></div>
          <div class="meddic-dot filled"></div>
          <div class="meddic-dot filled"></div>
          <div class="meddic-dot filled"></div>
          <div class="meddic-dot filled"></div>
          <div class="meddic-dot"></div>
          <div class="meddic-dot"></div>
        </div>
        <div class="card-meta">HubSpot · Auto-synced</div>
      </div>

      <!-- Card 4: Manager Notified -->
      <div class="card coral" id="card4">
        <div class="card-header">
          <div class="card-icon">
            <svg viewBox="0 0 24 24">
              <path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"/><path d="M13.73 21a2 2 0 0 1-3.46 0"/>
            </svg>
          </div>
          <span class="card-badge">Risk Alert</span>
        </div>
        <span class="card-title">Manager notified</span>
        <div class="card-content">
          <div class="quote-text">"Budget is tight this quarter, but we're interested in Q1."</div>
        </div>
        <div class="card-meta">Slack #deals · @mike</div>
      </div>
    </div>
  </section>

  <div class="theme-toggle">
    <button id="btn-dark" class="active" onclick="setTheme('dark')">Dark</button>
    <button id="btn-light" onclick="setTheme('light')">Light</button>
  </div>

  <script>
    import { gsap } from 'gsap';
    import Lenis from 'lenis';

    function setTheme(theme) {
      document.documentElement.setAttribute('data-theme', theme);
      document.getElementById('btn-dark')?.classList.toggle('active', theme === 'dark');
      document.getElementById('btn-light')?.classList.toggle('active', theme === 'light');
    }
    window.setTheme = setTheme;

    const lenis = new Lenis({
      duration: 1.2,
      easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t)),
      smoothWheel: true,
    });
    function raf(time) { lenis.raf(time); requestAnimationFrame(raf); }
    requestAnimationFrame(raf);

    function createTimeline() {
      const paths = document.querySelectorAll('.flow-path');
      
      paths.forEach(path => {
        const length = path.getTotalLength();
        path.style.strokeDasharray = length;
        path.style.strokeDashoffset = length;
      });

      gsap.set('.card', { opacity: 0, y: 30, scale: 0.95 });

      // No loop - plays once
      const tl = gsap.timeline();

      // Card 1 appears first
      tl.to('#card1', { opacity: 1, y: 0, scale: 1, duration: 0.8, ease: 'power2.out' });
      
      // Pause to read
      tl.to({}, { duration: 1 });
      
      // Path 1 draws AFTER card 1 is visible
      tl.to('#path1', { strokeDashoffset: 0, duration: 1.2, ease: 'power1.inOut' });
      
      // Card 2 appears at end of path
      tl.to('#card2', { opacity: 1, y: 0, scale: 1, duration: 0.8, ease: 'power2.out' }, '-=0.2');
      
      // Pause to read
      tl.to({}, { duration: 1.2 });
      
      // Path 2 draws
      tl.to('#path2', { strokeDashoffset: 0, duration: 1, ease: 'power1.inOut' });
      
      // Card 3 appears
      tl.to('#card3', { opacity: 1, y: 0, scale: 1, duration: 0.8, ease: 'power2.out' }, '-=0.2');
      
      // Pause to read
      tl.to({}, { duration: 1.2 });
      
      // Path 3 draws
      tl.to('#path3', { strokeDashoffset: 0, duration: 1.2, ease: 'power1.inOut' });
      
      // Card 4 appears
      tl.to('#card4', { opacity: 1, y: 0, scale: 1, duration: 0.8, ease: 'power2.out' }, '-=0.2');

      return tl;
    }

    document.addEventListener('DOMContentLoaded', () => {
      setTimeout(createTimeline, 500);
    });
  </script>
</body>
</html>
