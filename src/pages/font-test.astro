---
// src/pages/font-test.astro
---
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>recrd — Font Testing</title>
  <style>
    /* ============================================
       FONT FACE DECLARATIONS
       Matched to your /public/fonts/ files
       ============================================ */
    
    /* OBJECT SANS (Pangram Pangram) - Headlines */
    @font-face {
      font-family: 'Object Sans';
      src: url('/fonts/PPObjectSans-Heavy.otf') format('opentype');
      font-weight: 900;
      font-style: normal;
      font-display: swap;
    }
    @font-face {
      font-family: 'Object Sans';
      src: url('/fonts/PPObjectSans-Regular.otf') format('opentype');
      font-weight: 400;
      font-style: normal;
      font-display: swap;
    }

    /* FRAKTION MONO (Pangram Pangram) - Code/Data */
    @font-face {
      font-family: 'Fraktion Mono';
      src: url('/fonts/PPFraktionMono-Regular.otf') format('opentype');
      font-weight: 400;
      font-style: normal;
      font-display: swap;
    }

    /* ROOBERT (Displaay) - Variable Font */
    @font-face {
      font-family: 'Roobert';
      src: url('/fonts/RoobertCollectionVF-TRIAL.woff2') format('woff2');
      font-weight: 100 900;
      font-style: normal;
      font-display: swap;
    }

    /* MATTER (Displaay) - Static Fonts */
    @font-face {
      font-family: 'Matter';
      src: url('/fonts/Matter-TRIAL-Light.otf') format('opentype');
      font-weight: 300;
      font-style: normal;
      font-display: swap;
    }
    @font-face {
      font-family: 'Matter';
      src: url('/fonts/Matter-TRIAL-Regular.otf') format('opentype');
      font-weight: 400;
      font-style: normal;
      font-display: swap;
    }
    @font-face {
      font-family: 'Matter';
      src: url('/fonts/Matter-TRIAL-Medium.ttf') format('truetype');
      font-weight: 500;
      font-style: normal;
      font-display: swap;
    }

    /* ============================================
       BASE STYLES
       ============================================ */
    * { margin: 0; padding: 0; box-sizing: border-box; }
    
    :root {
      --mint: #46FCAF;
      --purple: #A678F4;
      --cyan: #02C0FF;
      --coral: #FF4F5E;
      --yellow: #FFE869;
      --orange: #FF8B5E;
      --surface: #1C1F22;
      --bg: #0A0A0A;
    }

    body {
      background: var(--bg);
      color: white;
      padding: 60px 40px;
      line-height: 1.5;
      font-family: system-ui, sans-serif;
    }

    h1 {
      font-family: 'Object Sans', system-ui, sans-serif;
      font-weight: 900;
      font-size: 32px;
      text-align: center;
      margin-bottom: 8px;
      letter-spacing: -0.02em;
    }

    .subtitle {
      text-align: center;
      color: #71717a;
      font-size: 14px;
      margin-bottom: 60px;
    }

    .section {
      margin-bottom: 80px;
    }

    .section-label {
      font-size: 11px;
      font-weight: 600;
      color: var(--mint);
      text-transform: uppercase;
      letter-spacing: 1.5px;
      margin-bottom: 24px;
    }

    .font-card {
      background: #111;
      border: 1px solid #1a1a1a;
      border-radius: 16px;
      padding: 40px;
      margin-bottom: 20px;
    }

    .font-header {
      display: flex;
      justify-content: space-between;
      align-items: baseline;
      margin-bottom: 32px;
      padding-bottom: 20px;
      border-bottom: 1px solid #1a1a1a;
    }

    .font-name {
      font-family: 'Object Sans', system-ui, sans-serif;
      font-weight: 900;
      font-size: 24px;
    }

    .font-meta {
      font-size: 12px;
      color: #52525b;
    }

    /* ============================================
       HEADLINE TESTS (Object Sans)
       ============================================ */
    .headline-heavy {
      font-family: 'Object Sans', system-ui, sans-serif;
      font-weight: 900;
      font-size: 56px;
      letter-spacing: -0.03em;
      line-height: 1.0;
      margin-bottom: 24px;
    }

    .headline-regular {
      font-family: 'Object Sans', system-ui, sans-serif;
      font-weight: 400;
      font-size: 28px;
      letter-spacing: -0.01em;
      line-height: 1.2;
      margin-bottom: 24px;
    }

    /* ============================================
       BODY FONT TESTS
       ============================================ */
    .body-test {
      margin-bottom: 32px;
    }

    .body-label {
      font-size: 10px;
      color: #52525b;
      text-transform: uppercase;
      letter-spacing: 1px;
      margin-bottom: 8px;
    }

    /* Roobert (Variable) */
    .roobert-light {
      font-family: 'Roobert', system-ui, sans-serif;
      font-weight: 300;
      font-size: 16px;
      line-height: 1.7;
      color: #a1a1aa;
    }
    .roobert-regular {
      font-family: 'Roobert', system-ui, sans-serif;
      font-weight: 400;
      font-size: 16px;
      line-height: 1.7;
      color: #d4d4d8;
    }
    .roobert-medium {
      font-family: 'Roobert', system-ui, sans-serif;
      font-weight: 500;
      font-size: 16px;
      line-height: 1.7;
      color: #e4e4e7;
    }
    .roobert-light-large {
      font-family: 'Roobert', system-ui, sans-serif;
      font-weight: 300;
      font-size: 22px;
      line-height: 1.6;
      color: #a1a1aa;
    }

    /* Matter */
    .matter-light {
      font-family: 'Matter', system-ui, sans-serif;
      font-weight: 300;
      font-size: 16px;
      line-height: 1.7;
      color: #a1a1aa;
    }
    .matter-regular {
      font-family: 'Matter', system-ui, sans-serif;
      font-weight: 400;
      font-size: 16px;
      line-height: 1.7;
      color: #d4d4d8;
    }
    .matter-medium {
      font-family: 'Matter', system-ui, sans-serif;
      font-weight: 500;
      font-size: 16px;
      line-height: 1.7;
      color: #e4e4e7;
    }
    .matter-light-large {
      font-family: 'Matter', system-ui, sans-serif;
      font-weight: 300;
      font-size: 22px;
      line-height: 1.6;
      color: #a1a1aa;
    }

    /* ============================================
       MONO TESTS
       ============================================ */
    .mono-code {
      font-family: 'Fraktion Mono', monospace;
      font-size: 14px;
      color: var(--mint);
      background: #0f0f0f;
      padding: 16px 20px;
      border-radius: 8px;
      border: 1px solid #1a1a1a;
      display: block;
      margin-bottom: 16px;
    }

    .mono-data {
      font-family: 'Fraktion Mono', monospace;
      font-size: 13px;
      color: #a1a1aa;
    }

    /* ============================================
       HERO PREVIEW
       ============================================ */
    .hero-preview {
      background: var(--bg);
      border: 1px solid #1a1a1a;
      border-radius: 20px;
      padding: 80px 60px;
      position: relative;
      overflow: hidden;
    }

    .hero-preview::before {
      content: '';
      position: absolute;
      top: -200px;
      right: -100px;
      width: 500px;
      height: 500px;
      background: radial-gradient(circle, rgba(70, 252, 175, 0.1) 0%, transparent 60%);
      pointer-events: none;
    }

    .hero-preview::after {
      content: '';
      position: absolute;
      bottom: -150px;
      left: -50px;
      width: 400px;
      height: 400px;
      background: radial-gradient(circle, rgba(166, 120, 244, 0.08) 0%, transparent 60%);
      pointer-events: none;
    }

    .preview-badge {
      font-size: 10px;
      color: var(--purple);
      text-transform: uppercase;
      letter-spacing: 1.5px;
      margin-bottom: 16px;
      position: relative;
    }

    .preview-headline {
      font-family: 'Object Sans', system-ui, sans-serif;
      font-weight: 900;
      font-size: 52px;
      letter-spacing: -0.03em;
      line-height: 1.05;
      margin-bottom: 24px;
      position: relative;
    }

    .preview-headline em {
      color: var(--mint);
      font-style: normal;
    }

    /* Body font switcher classes */
    .preview-sub.use-roobert {
      font-family: 'Roobert', system-ui, sans-serif;
      font-weight: 300;
    }
    .preview-sub.use-matter {
      font-family: 'Matter', system-ui, sans-serif;
      font-weight: 300;
    }

    .preview-sub {
      font-size: 20px;
      color: #a1a1aa;
      line-height: 1.6;
      margin-bottom: 32px;
      max-width: 520px;
      position: relative;
    }

    .preview-cta {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: var(--mint);
      color: #0a0a0a;
      font-family: 'Object Sans', system-ui, sans-serif;
      font-weight: 400;
      font-size: 14px;
      padding: 14px 24px;
      border: none;
      cursor: pointer;
      position: relative;
    }

    /* ============================================
       FONT SWITCHER
       ============================================ */
    .switcher {
      display: flex;
      gap: 12px;
      margin-bottom: 40px;
    }

    .switcher button {
      background: #1a1a1a;
      border: 1px solid #2a2a2a;
      color: #71717a;
      padding: 12px 20px;
      font-size: 14px;
      cursor: pointer;
      transition: all 0.2s;
      border-radius: 0;
    }

    .switcher button:hover {
      border-color: #3a3a3a;
      color: #a1a1aa;
    }

    .switcher button.active {
      background: var(--mint);
      border-color: var(--mint);
      color: #0a0a0a;
    }

    /* ============================================
       DATA CARDS
       ============================================ */
    .data-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 16px;
      margin-top: 40px;
      position: relative;
    }

    .data-card {
      background: #111;
      border: 1px solid #1a1a1a;
      border-radius: 12px;
      padding: 20px;
    }

    .data-label {
      font-family: 'Fraktion Mono', monospace;
      font-size: 10px;
      color: #52525b;
      text-transform: uppercase;
      letter-spacing: 1px;
      margin-bottom: 8px;
    }

    .data-value {
      font-family: 'Fraktion Mono', monospace;
      font-size: 24px;
    }

    .data-value.mint { color: var(--mint); }
    .data-value.purple { color: var(--purple); }
    .data-value.cyan { color: var(--cyan); }
    .data-value.white { color: #e4e4e7; }

    /* ============================================
       STATUS NOTE
       ============================================ */
    .note {
      background: rgba(166, 120, 244, 0.1);
      border: 1px solid rgba(166, 120, 244, 0.3);
      border-radius: 8px;
      padding: 16px 20px;
      margin-bottom: 40px;
      font-size: 14px;
      color: #a1a1aa;
    }
    .note strong {
      color: var(--purple);
    }
  </style>
</head>
<body>

  <h1>recrd Font Testing</h1>
  <p class="subtitle">Object Sans + Roobert / Matter + Fraktion Mono</p>

  <div class="note">
    <strong>Loaded fonts:</strong> Object Sans (Heavy, Regular) · Roobert (Variable) · Matter (Light, Regular, Medium) · Fraktion Mono
  </div>

  <!-- HEADLINES: Object Sans -->
  <div class="section">
    <div class="section-label">Headlines — Object Sans (Pangram Pangram)</div>
    <div class="font-card">
      <div class="headline-heavy">Every conversation triggers an army of AI agents</div>
      <div class="headline-regular">AI agents that actually understand your deals</div>
    </div>
  </div>

  <!-- BODY: Roobert -->
  <div class="section">
    <div class="section-label">Body Option A — Roobert (Displaay)</div>
    <div class="font-card">
      <div class="font-header">
        <span class="font-name">Roobert</span>
        <span class="font-meta">Variable font · Mono-linear geometric · Bent-pipe motif</span>
      </div>
      <div class="body-test">
        <div class="body-label">Light 300 (hero sub)</div>
        <p class="roobert-light-large">One call. Six agents. Zero setup. They draft follow-ups, update your CRM, score deals, flag risks, and coach your team — automatically.</p>
      </div>
      <div class="body-test">
        <div class="body-label">Regular 400 (body)</div>
        <p class="roobert-regular">Every conversation is automatically analyzed for sentiment, action items, and deal signals. Our AI agents work in the background to keep your CRM updated, draft follow-up emails, and surface coaching opportunities.</p>
      </div>
      <div class="body-test">
        <div class="body-label">Light 300 (secondary)</div>
        <p class="roobert-light">Calls are processed in real-time with results available within seconds of completion. No manual work required.</p>
      </div>
      <div class="body-test">
        <div class="body-label">Medium 500 (UI emphasis)</div>
        <p class="roobert-medium">Button labels, navigation items, and emphasized text.</p>
      </div>
    </div>
  </div>

  <!-- BODY: Matter -->
  <div class="section">
    <div class="section-label">Body Option B — Matter (Displaay)</div>
    <div class="font-card">
      <div class="font-header">
        <span class="font-name">Matter</span>
        <span class="font-meta">Warm grotesque · Diagonal terminals · Helvetica/Avenir roots</span>
      </div>
      <div class="body-test">
        <div class="body-label">Light 300 (hero sub)</div>
        <p class="matter-light-large">One call. Six agents. Zero setup. They draft follow-ups, update your CRM, score deals, flag risks, and coach your team — automatically.</p>
      </div>
      <div class="body-test">
        <div class="body-label">Regular 400 (body)</div>
        <p class="matter-regular">Every conversation is automatically analyzed for sentiment, action items, and deal signals. Our AI agents work in the background to keep your CRM updated, draft follow-up emails, and surface coaching opportunities.</p>
      </div>
      <div class="body-test">
        <div class="body-label">Light 300 (secondary)</div>
        <p class="matter-light">Calls are processed in real-time with results available within seconds of completion. No manual work required.</p>
      </div>
      <div class="body-test">
        <div class="body-label">Medium 500 (UI emphasis)</div>
        <p class="matter-medium">Button labels, navigation items, and emphasized text.</p>
      </div>
    </div>
  </div>

  <!-- CODE/DATA: Fraktion Mono -->
  <div class="section">
    <div class="section-label">Code & Data — Fraktion Mono (Pangram Pangram)</div>
    <div class="font-card">
      <code class="mono-code">const signals = await recrd.analyze(callId);</code>
      <p class="mono-data">Deal Score: 87 · Sentiment: +0.72 · Action Items: 4 · Duration: 32:14</p>
    </div>
  </div>

  <!-- HERO PREVIEW WITH SWITCHER -->
  <div class="section">
    <div class="section-label">Hero Preview — Switch Body Fonts</div>
    
    <div class="switcher">
      <button class="active" onclick="switchFont('roobert')">Roobert</button>
      <button onclick="switchFont('matter')">Matter</button>
    </div>

    <div class="hero-preview">
      <div class="preview-badge">Now in Beta</div>
      <h2 class="preview-headline">Every conversation triggers an army of <em>AI agents</em></h2>
      <p class="preview-sub use-roobert" id="preview-sub">One call. Six agents. Zero setup. They draft follow-ups, update your CRM, score deals, flag risks, and coach your team — automatically.</p>
      <button class="preview-cta">Get Started</button>
      
      <div class="data-grid">
        <div class="data-card">
          <div class="data-label">Deal Score</div>
          <div class="data-value mint">87</div>
        </div>
        <div class="data-card">
          <div class="data-label">Sentiment</div>
          <div class="data-value purple">+0.72</div>
        </div>
        <div class="data-card">
          <div class="data-label">Action Items</div>
          <div class="data-value cyan">4</div>
        </div>
        <div class="data-card">
          <div class="data-label">Duration</div>
          <div class="data-value white">32:14</div>
        </div>
      </div>
    </div>
  </div>

  <script>
    function switchFont(font) {
      const sub = document.getElementById('preview-sub');
      const buttons = document.querySelectorAll('.switcher button');
      
      buttons.forEach(btn => btn.classList.remove('active'));
      event.target.classList.add('active');
      
      sub.className = 'preview-sub use-' + font;
    }
  </script>

</body>
</html>
