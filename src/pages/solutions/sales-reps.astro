---
import Nav from '../../components/Nav.astro';
import Footer from '../../components/Footer.astro';
import ThemeToggle from '../../components/ThemeToggle.astro';
import BaseHead from '../../components/BaseHead.astro';
---

<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
  <BaseHead title="recrd for Sales Reps - Stop Doing CRM Busywork" description="Save 30 minutes after every call. recrd automatically updates your CRM, drafts follow-ups, and lets you focus on selling." />
  
  <style>
    .hero { padding: 160px 48px 100px; max-width: 1200px; margin: 0 auto; display: grid; grid-template-columns: 1fr 1fr; gap: 80px; align-items: center; }
    .hero-content { max-width: 540px; }
    .hero-badge { display: inline-flex; align-items: center; gap: 8px; padding: 8px 16px; background: var(--bg-card); border: 1px solid var(--border); border-radius: 6px; font-family: var(--mono); font-size: 12px; text-transform: uppercase; letter-spacing: 0.1em; margin-bottom: 24px; color: var(--mint); }
    [data-theme="light"] .hero-badge { color: var(--purple); }
    .hero-headline { font-size: 56px; font-weight: 350; line-height: 1.1; letter-spacing: -0.03em; margin-bottom: 24px; }
    .hero-headline em { font-style: normal; color: var(--mint); }
    [data-theme="light"] .hero-headline em { color: var(--purple); }
    .hero-sub { font-size: 18px; color: var(--text-muted); line-height: 1.6; margin-bottom: 40px; }
    .hero-cta { display: inline-flex; align-items: center; gap: 12px; padding: 18px 32px; font-weight: 500; font-size: 15px; text-decoration: none; position: relative; overflow: hidden; background: transparent; color: var(--text); border: 1px solid var(--btn-outline); border-radius: 6px; transition: all 0.3s ease; }
    .hero-cta::before { content: ''; position: absolute; top: 0; left: 0; width: 0; height: 100%; background: var(--mint); transition: width 0.3s ease; z-index: -1; }
    .hero-cta:hover::before { width: 100%; }
    .hero-cta:hover { color: #0a0a0a; border-color: var(--mint); }
    .hero-cta-arrow { transition: transform 0.2s ease; }
    .hero-cta-arrow img { width: 16px; height: 16px; }
    .hero-cta:hover .hero-cta-arrow { transform: translateX(4px); }
    
    /* Hero CTA chevron hover swap */
    .hero-cta-arrow .chevron-hover { display: none; }
    .hero-cta:hover .chevron-default { display: none; }
    .hero-cta:hover .chevron-hover { display: block; }

    .hero-visual { background: var(--bg-card); border: 1px solid var(--border); border-radius: 6px; padding: 32px; }
    .hero-mock-header { display: flex; align-items: center; justify-content: space-between; padding-bottom: 16px; border-bottom: 1px solid var(--border); margin-bottom: 20px; }
    .hero-mock-title { font-weight: 600; font-size: 14px; }
    .hero-mock-time { font-family: var(--mono); font-size: 11px; color: var(--mint); }
    .hero-task { display: flex; align-items: center; gap: 12px; padding: 14px 0; border-bottom: 1px solid var(--border); }
    .hero-task:last-child { border-bottom: none; }
    .hero-task-check { width: 20px; height: 20px; background: var(--mint); display: flex; align-items: center; justify-content: center; }
    .hero-task-check svg { width: 12px; height: 12px; stroke: #0a0a0a; stroke-width: 3; }
    .hero-task-text { font-size: 14px; flex: 1; }
    .hero-task-status { font-family: var(--mono); font-size: 11px; color: var(--mint); }

    .pain-section { padding: 100px 48px; border-top: 1px solid var(--border); background: var(--bg-alt); }
    .pain-header { text-align: center; max-width: 600px; margin: 0 auto 64px; }
    .pain-label { font-family: var(--mono); font-size: 12px; text-transform: uppercase; letter-spacing: 0.1em; color: var(--coral); margin-bottom: 16px; }
    .pain-headline { font-size: 36px; font-weight: 350; line-height: 1.2; letter-spacing: -0.02em; }
    .pain-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1px; background: var(--border); max-width: 1000px; margin: 0 auto; border-radius: 6px; overflow: hidden; }
    .pain-card { background: var(--bg); padding: 40px 32px; text-align: center; }
    .pain-stat { font-size: 48px; font-weight: 300; color: var(--coral); margin-bottom: 12px; font-family: var(--mono); }
    .pain-desc { font-size: 15px; color: var(--text-muted); line-height: 1.5; }

    .solution-section { padding: 120px 48px; border-top: 1px solid var(--border); }
    .solution-header { text-align: center; max-width: 600px; margin: 0 auto 80px; }
    .solution-label { font-family: var(--mono); font-size: 12px; text-transform: uppercase; letter-spacing: 0.1em; color: var(--mint); margin-bottom: 16px; }
    [data-theme="light"] .solution-label { color: var(--purple); }
    .solution-headline { font-size: 36px; font-weight: 350; line-height: 1.2; letter-spacing: -0.02em; margin-bottom: 16px; }
    .solution-sub { font-size: 16px; color: var(--text-muted); }
    .solution-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 24px; max-width: 1000px; margin: 0 auto; }
    .solution-card { padding: 40px; background: var(--bg-card); border: 1px solid var(--border); border-radius: 6px; }
    .solution-icon { width: 48px; height: 48px; margin-bottom: 24px; }
    .solution-icon img { width: 100%; height: 100%; object-fit: contain; }
    .solution-title { font-size: 20px; font-weight: 600; margin-bottom: 12px; }
    .solution-desc { font-size: 15px; color: var(--text-muted); line-height: 1.6; }

    .quote-section { padding: 100px 48px; border-top: 1px solid var(--border); background: var(--bg-alt); }
    .quote-container { max-width: 800px; margin: 0 auto; text-align: center; }
    .quote-mark { font-size: 80px; line-height: 1; color: var(--mint); opacity: 0.3; margin-bottom: -20px; }
    [data-theme="light"] .quote-mark { color: var(--purple); }
    .quote-text { font-size: 28px; font-weight: 350; line-height: 1.5; letter-spacing: -0.01em; margin-bottom: 32px; }
    .quote-author { font-size: 15px; color: var(--text-muted); }
    .quote-author strong { color: var(--text); font-weight: 600; }

    .cta-section { padding: 120px 48px; text-align: center; border-top: 1px solid var(--border); }
    .cta-headline { font-size: 40px; font-weight: 350; line-height: 1.2; letter-spacing: -0.02em; margin-bottom: 16px; }
    .cta-sub { font-size: 18px; color: var(--text-muted); margin-bottom: 40px; max-width: 500px; margin-left: auto; margin-right: auto; }
    .btn { display: inline-flex; align-items: center; gap: 12px; padding: 18px 28px; font-weight: 500; font-size: 15px; text-decoration: none; transition: all 0.2s ease; cursor: pointer; position: relative; overflow: hidden; border-radius: 6px; }
    .btn-primary { background: transparent; color: var(--text); border: 1px solid var(--btn-outline); }
    .btn-primary::before { content: ''; position: absolute; top: 0; left: 0; width: 0; height: 100%; background: var(--mint); transition: width 0.3s ease; z-index: -1; }
    .btn-primary:hover::before { width: 100%; }
    .btn-primary:hover { color: #0a0a0a; border-color: var(--mint); }
    .btn-arrow { display: inline-flex; transition: transform 0.2s ease; }
    .btn-arrow img { width: 16px; height: 16px; }
    .btn:hover .btn-arrow { transform: translateX(4px); }
    
    /* Chevron hover swap */
    .btn-arrow .chevron-hover { display: none; }
    .btn:hover .chevron-default { display: none; }
    .btn:hover .chevron-hover { display: block; }

    @media (max-width: 1000px) { .hero { grid-template-columns: 1fr; gap: 48px; text-align: center; } .hero-content { max-width: 100%; } .solution-grid { grid-template-columns: 1fr; } }
    @media (max-width: 900px) { .hero { padding: 140px 24px 80px; } .hero-headline { font-size: 40px; } .pain-section { padding: 80px 24px; } .pain-grid { grid-template-columns: 1fr; } .pain-headline { font-size: 28px; } .solution-section { padding: 80px 24px; } .solution-headline { font-size: 28px; } .quote-section { padding: 80px 24px; } .quote-text { font-size: 22px; } .cta-section { padding: 80px 24px; } .cta-headline { font-size: 32px; } }
    @media (max-width: 600px) { .hero-headline { font-size: 32px; } .pain-stat { font-size: 36px; } }
  </style>
</head>

<body>
  <Nav />

  <section class="hero">
    <div class="hero-content">
      <div class="hero-badge">For Sales Reps</div>
      <h1 class="hero-headline">Stop doing <em>CRM busywork</em></h1>
      <p class="hero-sub">You became a rep to sell, not to spend 30 minutes after every call updating HubSpot. recrd does it for you—automatically.</p>
      <a href="#" class="hero-cta">Get Started Free <span class="hero-cta-arrow"><img src="/icons/chevron-green.svg" alt="" class="chevron-default" /><img src="/icons/chevron-black.svg" alt="" class="chevron-hover" /></span></a>
    </div>
    <div class="hero-visual">
      <div class="hero-mock-header"><span class="hero-mock-title">Post-Call Tasks</span><span class="hero-mock-time">Auto-completed in 47s</span></div>
      <div class="hero-task"><div class="hero-task-check"><svg viewBox="0 0 24 24" fill="none"><polyline points="20 6 9 17 4 12"/></svg></div><span class="hero-task-text">Update deal stage in HubSpot</span><span class="hero-task-status">Done</span></div>
      <div class="hero-task"><div class="hero-task-check"><svg viewBox="0 0 24 24" fill="none"><polyline points="20 6 9 17 4 12"/></svg></div><span class="hero-task-text">Log MEDDIC qualification fields</span><span class="hero-task-status">Done</span></div>
      <div class="hero-task"><div class="hero-task-check"><svg viewBox="0 0 24 24" fill="none"><polyline points="20 6 9 17 4 12"/></svg></div><span class="hero-task-text">Draft follow-up email</span><span class="hero-task-status">Done</span></div>
      <div class="hero-task"><div class="hero-task-check"><svg viewBox="0 0 24 24" fill="none"><polyline points="20 6 9 17 4 12"/></svg></div><span class="hero-task-text">Set next step reminder</span><span class="hero-task-status">Done</span></div>
    </div>
  </section>

  <section class="pain-section">
    <div class="pain-header"><p class="pain-label">The Problem</p><h2 class="pain-headline">Admin work is killing your quota</h2></div>
    <div class="pain-grid">
      <div class="pain-card"><div class="pain-stat">30m</div><p class="pain-desc">Average time reps spend on CRM updates after each call</p></div>
      <div class="pain-card"><div class="pain-stat">5.2h</div><p class="pain-desc">Weekly hours lost to admin work instead of selling</p></div>
      <div class="pain-card"><div class="pain-stat">23%</div><p class="pain-desc">Of rep time actually spent talking to prospects</p></div>
    </div>
  </section>

  <section class="solution-section">
    <div class="solution-header"><p class="solution-label">How recrd helps</p><h2 class="solution-headline">Get back to what you do best</h2><p class="solution-sub">Every call recorded, transcribed, and processed—so you can focus on closing.</p></div>
    <div class="solution-grid">
      <div class="solution-card">
        <div class="solution-icon"><img src="/icons/checklist.svg" alt="CRM Updates" /></div>
        <h3 class="solution-title">Auto CRM Updates</h3>
        <p class="solution-desc">Deal stage, MEDDIC fields, next steps, competitor mentions—all synced to your CRM within 60 seconds of hanging up.</p>
      </div>
      <div class="solution-card">
        <div class="solution-icon"><img src="/icons/email-campaign.svg" alt="Follow-Up" /></div>
        <h3 class="solution-title">Follow-Up Drafts</h3>
        <p class="solution-desc">Personalized follow-up emails ready before you finish your coffee. References specific topics, next steps, and action items.</p>
      </div>
      <div class="solution-card">
        <div class="solution-icon"><img src="/icons/meeting-prep-green.svg" alt="Meeting Prep" /></div>
        <h3 class="solution-title">Meeting Prep</h3>
        <p class="solution-desc">Get AI-generated briefs before every call. Past interactions, stakeholder context, talking points—ready when you are.</p>
      </div>
      <div class="solution-card">
        <div class="solution-icon"><img src="/icons/agent-forecast.svg" alt="Deal Intelligence" /></div>
        <h3 class="solution-title">Deal Intelligence</h3>
        <p class="solution-desc">See your deal health at a glance. Risk signals, buying signals, and exactly what to focus on to move deals forward.</p>
      </div>
    </div>
  </section>

  <section class="quote-section">
    <div class="quote-container"><div class="quote-mark">"</div><p class="quote-text">I used to spend my evenings catching up on CRM. Now I finish my notes before the next meeting starts. recrd gave me my life back.</p><p class="quote-author"><strong>Alex M.</strong> — Enterprise AE</p></div>
  </section>

  <section class="cta-section">
    <h2 class="cta-headline">Ready to sell more, admin less?</h2>
    <p class="cta-sub">Join thousands of reps who save 5+ hours every week with recrd.</p>
    <a href="#" class="btn btn-primary">Start Free Trial <span class="btn-arrow"><img src="/icons/chevron-green.svg" alt="" class="chevron-default" /><img src="/icons/chevron-black.svg" alt="" class="chevron-hover" /></span></a>
  </section>

  <Footer />
  <ThemeToggle />
</body>
</html>
