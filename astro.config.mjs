import { defineConfig } from 'astro/config';
export default defineConfig({ site: 'https://recrd.io' });
```

2. **`public/` folder** — optional but add it for images/fonts later:
```
public/
  assets/
    images/
    fonts/
