---
// src/pages/font-test.astro
---
<html lang="en" data-theme="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>recrd — Font Test</title>
  <style>
    @font-face {
      font-family: 'Roobert';
      src: url('/fonts/RoobertCollectionVF-TRIAL.woff2') format('woff2');
      font-weight: 100 900;
      font-display: swap;
    }
    @font-face {
      font-family: 'Matter';
      src: url('/fonts/Matter-TRIAL-Light.otf') format('opentype');
      font-weight: 300;
      font-display: swap;
    }
    @font-face {
      font-family: 'Matter';
      src: url('/fonts/Matter-TRIAL-Regular.otf') format('opentype');
      font-weight: 400;
      font-display: swap;
    }
    @font-face {
      font-family: 'Matter';
      src: url('/fonts/Matter-TRIAL-Medium.ttf') format('truetype');
      font-weight: 500;
      font-display: swap;
    }
    @font-face {
      font-family: 'Object Sans';
      src: url('/fonts/PPObjectSans-Heavy.otf') format('opentype');
      font-weight: 900;
      font-display: swap;
    }
    @font-face {
      font-family: 'Object Sans';
      src: url('/fonts/PPObjectSans-Regular.otf') format('opentype');
      font-weight: 400;
      font-display: swap;
    }
    @font-face {
      font-family: 'Fraktion Mono';
      src: url('/fonts/PPFraktionMono-Regular.otf') format('opentype');
      font-weight: 400;
      font-display: swap;
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }

    [data-theme="dark"] {
      --bg: #0A0A0A;
      --text: #ffffff;
      --text-dim: #888;
      --border: #222;
      --accent: #46FCAF;
    }
    [data-theme="light"] {
      --bg: #fafafa;
      --text: #0a0a0a;
      --text-dim: #666;
      --border: #ddd;
      --accent: #46FCAF;
    }

    body {
      background: var(--bg);
      color: var(--text);
      font-family: system-ui, sans-serif;
      padding: 40px;
      transition: background 0.2s, color 0.2s;
    }

    .controls {
      position: fixed;
      top: 20px;
      right: 20px;
      display: flex;
      gap: 8px;
      background: var(--bg);
      border: 1px solid var(--border);
      padding: 12px;
      border-radius: 8px;
      z-index: 100;
    }

    .controls button {
      background: transparent;
      border: 1px solid var(--border);
      color: var(--text-dim);
      padding: 6px 12px;
      font-size: 11px;
      font-family: 'Fraktion Mono', monospace;
      cursor: pointer;
      border-radius: 4px;
    }
    .controls button:hover { color: var(--text); }
    .controls button.active {
      background: var(--accent);
      color: #0a0a0a;
      border-color: var(--accent);
    }

    h1 {
      font-family: 'Fraktion Mono', monospace;
      font-size: 11px;
      color: var(--text-dim);
      text-transform: uppercase;
      letter-spacing: 2px;
      margin-bottom: 60px;
    }

    .font-section {
      margin-bottom: 80px;
      padding-bottom: 80px;
      border-bottom: 1px solid var(--border);
    }
    .font-section:last-child { border-bottom: none; }

    .font-name {
      font-family: 'Fraktion Mono', monospace;
      font-size: 10px;
      color: var(--accent);
      text-transform: uppercase;
      letter-spacing: 2px;
      margin-bottom: 24px;
    }

    .specimen { margin-bottom: 40px; }

    .specimen-label {
      font-family: 'Fraktion Mono', monospace;
      font-size: 9px;
      color: var(--text-dim);
      text-transform: uppercase;
      letter-spacing: 1px;
      margin-bottom: 8px;
    }

    .headline-specimen {
      font-size: 64px;
      line-height: 1;
      letter-spacing: -0.03em;
      margin-bottom: 16px;
    }

    .subhead-specimen {
      font-size: 32px;
      line-height: 1.2;
      letter-spacing: -0.02em;
      margin-bottom: 16px;
    }

    .body-specimen {
      font-size: 16px;
      line-height: 1.6;
      max-width: 600px;
      margin-bottom: 16px;
    }

    .weights {
      display: flex;
      gap: 40px;
      flex-wrap: wrap;
      margin-top: 32px;
    }

    .weight-sample {
      min-width: 150px;
    }

    .weight-label {
      font-family: 'Fraktion Mono', monospace;
      font-size: 9px;
      color: var(--text-dim);
      margin-bottom: 4px;
    }

    .weight-text {
      font-size: 24px;
    }

    /* Font families */
    .roobert { font-family: 'Roobert', system-ui, sans-serif; }
    .matter { font-family: 'Matter', system-ui, sans-serif; }
    .object { font-family: 'Object Sans', system-ui, sans-serif; }
    .fraktion { font-family: 'Fraktion Mono', monospace; }

    @media (max-width: 600px) {
      body { padding: 20px; }
      .controls { top: 10px; right: 10px; padding: 8px; }
      .headline-specimen { font-size: 40px; }
      .subhead-specimen { font-size: 24px; }
    }
  </style>
</head>
<body>

  <div class="controls">
    <button class="active" id="btn-dark" onclick="setTheme('dark')">Dark</button>
    <button id="btn-light" onclick="setTheme('light')">Light</button>
  </div>

  <h1>recrd Font Specimens</h1>

  <!-- ROOBERT -->
  <section class="font-section">
    <div class="font-name">Roobert (Variable 100-900)</div>
    
    <div class="specimen">
      <div class="specimen-label">Headline · 700</div>
      <div class="headline-specimen roobert" style="font-weight: 700;">AI agents that close deals</div>
    </div>

    <div class="specimen">
      <div class="specimen-label">Subhead · 500</div>
      <div class="subhead-specimen roobert" style="font-weight: 500;">Every conversation triggers an army of AI agents</div>
    </div>

    <div class="specimen">
      <div class="specimen-label">Body · 300</div>
      <div class="body-specimen roobert" style="font-weight: 300;">Record your sales calls. Let AI handle the follow-ups, CRM updates, and coaching insights — from your first call to your thousandth. One call. Six agents. Zero setup.</div>
    </div>

    <div class="weights">
      <div class="weight-sample">
        <div class="weight-label">100 Thin</div>
        <div class="weight-text roobert" style="font-weight: 100;">Agents</div>
      </div>
      <div class="weight-sample">
        <div class="weight-label">200 Extra Light</div>
        <div class="weight-text roobert" style="font-weight: 200;">Agents</div>
      </div>
      <div class="weight-sample">
        <div class="weight-label">300 Light</div>
        <div class="weight-text roobert" style="font-weight: 300;">Agents</div>
      </div>
      <div class="weight-sample">
        <div class="weight-label">400 Regular</div>
        <div class="weight-text roobert" style="font-weight: 400;">Agents</div>
      </div>
      <div class="weight-sample">
        <div class="weight-label">500 Medium</div>
        <div class="weight-text roobert" style="font-weight: 500;">Agents</div>
      </div>
      <div class="weight-sample">
        <div class="weight-label">600 Semi Bold</div>
        <div class="weight-text roobert" style="font-weight: 600;">Agents</div>
      </div>
      <div class="weight-sample">
        <div class="weight-label">700 Bold</div>
        <div class="weight-text roobert" style="font-weight: 700;">Agents</div>
      </div>
      <div class="weight-sample">
        <div class="weight-label">800 Extra Bold</div>
        <div class="weight-text roobert" style="font-weight: 800;">Agents</div>
      </div>
      <div class="weight-sample">
        <div class="weight-label">900 Black</div>
        <div class="weight-text roobert" style="font-weight: 900;">Agents</div>
      </div>
    </div>
  </section>

  <!-- MATTER -->
  <section class="font-section">
    <div class="font-name">Matter (300, 400, 500 only)</div>
    
    <div class="specimen">
      <div class="specimen-label">Headline · 500</div>
      <div class="headline-specimen matter" style="font-weight: 500;">AI agents that close deals</div>
    </div>

    <div class="specimen">
      <div class="specimen-label">Subhead · 400</div>
      <div class="subhead-specimen matter" style="font-weight: 400;">Every conversation triggers an army of AI agents</div>
    </div>

    <div class="specimen">
      <div class="specimen-label">Body · 300</div>
      <div class="body-specimen matter" style="font-weight: 300;">Record your sales calls. Let AI handle the follow-ups, CRM updates, and coaching insights — from your first call to your thousandth. One call. Six agents. Zero setup.</div>
    </div>

    <div class="weights">
      <div class="weight-sample">
        <div class="weight-label">300 Light</div>
        <div class="weight-text matter" style="font-weight: 300;">Agents</div>
      </div>
      <div class="weight-sample">
        <div class="weight-label">400 Regular</div>
        <div class="weight-text matter" style="font-weight: 400;">Agents</div>
      </div>
      <div class="weight-sample">
        <div class="weight-label">500 Medium</div>
        <div class="weight-text matter" style="font-weight: 500;">Agents</div>
      </div>
    </div>
  </section>

  <!-- OBJECT SANS -->
  <section class="font-section">
    <div class="font-name">PP Object Sans (400, 900 only)</div>
    
    <div class="specimen">
      <div class="specimen-label">Headline · 900 Heavy</div>
      <div class="headline-specimen object" style="font-weight: 900;">AI agents that close deals</div>
    </div>

    <div class="specimen">
      <div class="specimen-label">Subhead · 400</div>
      <div class="subhead-specimen object" style="font-weight: 400;">Every conversation triggers an army of AI agents</div>
    </div>

    <div class="specimen">
      <div class="specimen-label">Body · 400</div>
      <div class="body-specimen object" style="font-weight: 400;">Record your sales calls. Let AI handle the follow-ups, CRM updates, and coaching insights — from your first call to your thousandth. One call. Six agents. Zero setup.</div>
    </div>

    <div class="weights">
      <div class="weight-sample">
        <div class="weight-label">400 Regular</div>
        <div class="weight-text object" style="font-weight: 400;">Agents</div>
      </div>
      <div class="weight-sample">
        <div class="weight-label">900 Heavy</div>
        <div class="weight-text object" style="font-weight: 900;">Agents</div>
      </div>
    </div>
  </section>

  <!-- FRAKTION MONO -->
  <section class="font-section">
    <div class="font-name">PP Fraktion Mono (400 only)</div>
    
    <div class="specimen">
      <div class="specimen-label">Data / Code</div>
      <div class="subhead-specimen fraktion" style="font-weight: 400;">78% win probability</div>
    </div>

    <div class="specimen">
      <div class="specimen-label">Labels</div>
      <div class="body-specimen fraktion" style="font-weight: 400; font-size: 12px; text-transform: uppercase; letter-spacing: 2px;">LIVE · PROCESSING · 3 FIELDS UPDATED</div>
    </div>
  </section>

  <script is:inline>
    function setTheme(theme) {
      document.documentElement.setAttribute('data-theme', theme);
      document.getElementById('btn-dark').classList.toggle('active', theme === 'dark');
      document.getElementById('btn-light').classList.toggle('active', theme === 'light');
    }
  </script>

</body>
</html>
