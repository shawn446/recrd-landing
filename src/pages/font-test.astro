---
// Hero Section - Professional Grid Animation with GSAP + Lenis
---

<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>recrd - AI agents that close deals</title>
  
  <style>
    /* Fonts */
    @font-face {
      font-family: 'Roobert';
      src: url('/fonts/RoobertCollectionVF-TRIAL.woff2') format('woff2');
      font-weight: 100 900;
      font-style: normal;
      font-display: swap;
    }
    @font-face {
      font-family: 'Fraktion Mono';
      src: url('/fonts/PPFraktionMono-Regular.otf') format('opentype');
      font-weight: 400;
      font-style: normal;
      font-display: swap;
    }
    @font-face {
      font-family: 'Object Sans';
      src: url('/fonts/PPObjectSans-Regular.otf') format('opentype');
      font-weight: 400;
      font-style: normal;
      font-display: swap;
    }
    @font-face {
      font-family: 'Object Sans';
      src: url('/fonts/PPObjectSans-Heavy.otf') format('opentype');
      font-weight: 800;
      font-style: normal;
      font-display: swap;
    }
    @font-face {
      font-family: 'Matter';
      src: url('/fonts/Matter-TRIAL-Light.otf') format('opentype');
      font-weight: 300;
      font-style: normal;
      font-display: swap;
    }
    @font-face {
      font-family: 'Matter';
      src: url('/fonts/Matter-TRIAL-Regular.otf') format('opentype');
      font-weight: 400;
      font-style: normal;
      font-display: swap;
    }
    @font-face {
      font-family: 'Matter';
      src: url('/fonts/Matter-TRIAL-Medium.ttf') format('truetype');
      font-weight: 500;
      font-style: normal;
      font-display: swap;
    }

    :root {
      --font: 'Roobert', -apple-system, BlinkMacSystemFont, sans-serif;
      --mono: 'Fraktion Mono', 'SF Mono', monospace;
      --mint: #46FCAF;
      --purple: #A678F4;
      --cyan: #02C0FF;
      --coral: #FF4F5E;
      
      /* Grid dimensions - 100px cells in viewBox */
      --cell: 100px;
      --cols: 7;
      --rows: 6;
    }

    [data-theme="dark"] {
      --bg: #0A0A0A;
      --text: #ffffff;
      --text-muted: #888888;
      --grid-line: rgba(255,255,255,0.12);
    }

    [data-theme="light"] {
      --bg: #fafafa;
      --text: #0a0a0a;
      --text-muted: #666666;
      --grid-line: rgba(0,0,0,0.1);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html.lenis, html.lenis body {
      height: auto;
    }

    body {
      font-family: var(--font);
      background: var(--bg);
      color: var(--text);
      min-height: 100vh;
      overflow-x: hidden;
    }

    /* NAV */
    nav {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      height: 72px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0 48px;
      z-index: 100;
      background: var(--bg);
      border-bottom: 1px solid var(--grid-line);
    }

    .nav-logo {
      display: flex;
      align-items: center;
      gap: 12px;
      text-decoration: none;
    }

    .nav-logo svg {
      width: 32px;
      height: 32px;
    }

    .nav-wordmark {
      font-weight: 600;
      font-size: 18px;
      color: var(--text);
    }

    .nav-links {
      display: flex;
      gap: 40px;
      list-style: none;
    }

    .nav-links a {
      color: var(--text-muted);
      text-decoration: none;
      font-size: 14px;
      font-weight: 500;
      transition: color 0.2s;
    }

    .nav-links a:hover {
      color: var(--text);
    }

    .nav-cta {
      background: var(--text);
      color: var(--bg);
      padding: 12px 24px;
      font-weight: 500;
      font-size: 14px;
      text-decoration: none;
      transition: background 0.2s, color 0.2s;
    }

    .nav-cta:hover {
      background: var(--mint);
      color: #0a0a0a;
    }

    /* HERO */
    .hero {
      min-height: 100vh;
      display: flex;
      align-items: center;
      padding: 120px 48px 48px;
      position: relative;
      overflow: hidden;
    }

    .hero-content {
      max-width: 480px;
      z-index: 10;
    }

    .hero-headline {
      font-size: clamp(48px, 8vw, 88px);
      font-weight: 700;
      line-height: 0.95;
      letter-spacing: -0.035em;
      margin-bottom: 24px;
    }

    .hero-headline em {
      font-style: normal;
      color: var(--mint);
    }

    [data-theme="light"] .hero-headline em {
      color: var(--purple);
    }

    .hero-sub {
      font-size: clamp(24px, 3.5vw, 38px);
      font-weight: 500;
      line-height: 1.2;
      margin-bottom: 20px;
      letter-spacing: -0.02em;
    }

    .hero-body {
      font-size: 18px;
      font-weight: 300;
      color: var(--text-muted);
      line-height: 1.6;
      margin-bottom: 40px;
    }

    .hero-buttons {
      display: flex;
      gap: 16px;
    }

    .btn {
      padding: 16px 32px;
      font-family: var(--font);
      font-weight: 500;
      font-size: 15px;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      gap: 12px;
      transition: all 0.3s ease;
    }

    .btn-primary {
      background: #fff;
      color: #0a0a0a;
    }

    .btn-primary:hover {
      background: var(--mint);
    }

    .btn-secondary {
      background: transparent;
      color: var(--text);
      border: 1px solid var(--grid-line);
    }

    .btn-secondary:hover {
      border-color: var(--mint);
    }

    [data-theme="light"] .btn-secondary:hover {
      border-color: var(--purple);
    }

    /* ========================================
       GRID ANIMATION SYSTEM - ENLARGED
       ======================================== */
    
    .grid-container {
      position: absolute;
      top: 50%;
      right: 2%;
      transform: translateY(-50%);
      /* SCALED UP: 840x720 (was 700x600) */
      width: 840px;
      height: 720px;
    }

    .grid-svg {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      overflow: visible;
      z-index: 1;
    }

    /* Grid cell outlines */
    .grid-cell-bg {
      fill: none;
      stroke: var(--grid-line);
      stroke-width: 1;
    }

    /* Animated connecting lines */
    .grid-path {
      fill: none;
      stroke-width: 3;
      stroke-linecap: square;
      stroke-linejoin: miter;
    }

    /* Cell content boxes - positioned over SVG */
    .cell {
      position: absolute;
      display: flex;
      flex-direction: column;
      justify-content: center;
      padding: 20px;
      opacity: 0;
      transform: scale(0.92);
      z-index: 5;
      pointer-events: none;
    }

    .cell-header {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 10px;
    }

    .cell-icon {
      width: 24px;
      height: 24px;
      flex-shrink: 0;
    }

    .cell-icon svg {
      width: 100%;
      height: 100%;
    }

    .cell-badge {
      font-family: var(--mono);
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      padding: 5px 10px;
      width: fit-content;
    }

    .cell-title {
      font-size: 18px;
      font-weight: 600;
      line-height: 1.25;
      margin-bottom: 6px;
    }

    .cell-desc {
      font-size: 13px;
      font-weight: 400;
      line-height: 1.4;
      opacity: 0.85;
    }

    .cell-meta {
      font-family: var(--mono);
      font-size: 11px;
      margin-top: 8px;
      opacity: 0.7;
    }

    /* Cell color variants */
    .cell.mint {
      background: var(--mint);
      color: #0a0a0a;
    }
    .cell.mint .cell-badge {
      background: rgba(0,0,0,0.12);
    }
    .cell.mint .cell-icon svg {
      stroke: #0a0a0a;
      fill: none;
    }

    .cell.purple {
      background: var(--purple);
      color: #fff;
    }
    .cell.purple .cell-badge {
      background: rgba(255,255,255,0.2);
    }
    .cell.purple .cell-icon svg {
      stroke: #fff;
      fill: none;
    }

    .cell.cyan {
      background: var(--cyan);
      color: #0a0a0a;
    }
    .cell.cyan .cell-badge {
      background: rgba(0,0,0,0.12);
    }
    .cell.cyan .cell-icon svg {
      stroke: #0a0a0a;
      fill: none;
    }

    .cell.coral {
      background: var(--coral);
      color: #fff;
    }
    .cell.coral .cell-badge {
      background: rgba(255,255,255,0.2);
    }
    .cell.coral .cell-icon svg {
      stroke: #fff;
      fill: none;
    }

    /* Theme toggle */
    .theme-toggle {
      position: fixed;
      bottom: 24px;
      left: 50%;
      transform: translateX(-50%);
      display: flex;
      background: var(--bg);
      border: 1px solid var(--grid-line);
      z-index: 1000;
    }

    .theme-toggle button {
      padding: 10px 20px;
      background: none;
      border: none;
      color: var(--text-muted);
      font-family: var(--font);
      font-size: 13px;
      cursor: pointer;
      transition: all 0.2s;
    }

    .theme-toggle button.active {
      background: var(--text);
      color: var(--bg);
    }

    /* ========================================
       RESPONSIVE
       ======================================== */
    @media (max-width: 1600px) {
      .grid-container {
        width: 756px;
        height: 648px;
      }
    }

    @media (max-width: 1400px) {
      .grid-container {
        width: 630px;
        height: 540px;
        right: 2%;
      }
      .cell {
        padding: 14px;
      }
      .cell-title {
        font-size: 15px;
      }
      .cell-desc {
        font-size: 12px;
      }
      .cell-icon {
        width: 20px;
        height: 20px;
      }
    }

    @media (max-width: 1200px) {
      .grid-container {
        width: 504px;
        height: 432px;
        right: 1%;
      }
      .cell {
        padding: 12px;
      }
      .cell-header {
        gap: 8px;
        margin-bottom: 8px;
      }
      .cell-badge {
        font-size: 8px;
        padding: 4px 8px;
      }
      .cell-title {
        font-size: 13px;
        margin-bottom: 4px;
      }
      .cell-desc {
        font-size: 11px;
      }
      .cell-meta {
        display: none;
      }
      .cell-icon {
        width: 18px;
        height: 18px;
      }
    }

    @media (max-width: 1000px) {
      .grid-container {
        width: 420px;
        height: 360px;
      }
      .cell-desc {
        display: none;
      }
      .cell-title {
        font-size: 12px;
      }
      .cell-icon {
        width: 16px;
        height: 16px;
      }
    }

    @media (max-width: 900px) {
      .hero {
        flex-direction: column;
        justify-content: center;
        text-align: center;
        padding: 120px 24px 48px;
      }
      .hero-content {
        max-width: 100%;
      }
      .hero-buttons {
        justify-content: center;
      }
      .grid-container {
        position: relative;
        top: auto;
        right: auto;
        transform: none;
        margin-top: 48px;
        width: 100%;
        max-width: 560px;
        height: auto;
        aspect-ratio: 7/6;
      }
      nav {
        padding: 0 24px;
      }
      .nav-links {
        display: none;
      }
    }

    @media (max-width: 600px) {
      .hero-headline {
        font-size: 40px;
      }
      .hero-sub {
        font-size: 22px;
      }
      .hero-body {
        font-size: 16px;
      }
      .btn {
        padding: 14px 24px;
        font-size: 14px;
      }
      .cell {
        padding: 8px;
      }
      .cell-badge {
        font-size: 7px;
        padding: 3px 6px;
      }
      .cell-title {
        font-size: 10px;
      }
      .cell-header {
        gap: 6px;
        margin-bottom: 6px;
      }
      .cell-icon {
        width: 14px;
        height: 14px;
      }
    }
  </style>
</head>

<body>
  <nav>
    <a href="/" class="nav-logo">
      <svg viewBox="0 0 52 52" fill="none">
        <rect y="16" width="28" height="28" fill="#A678F4"/>
        <rect x="16" width="28" height="28" rx="6" fill="#46FCAF"/>
      </svg>
      <span class="nav-wordmark">recrd</span>
    </a>
    <ul class="nav-links">
      <li><a href="#">Features</a></li>
      <li><a href="#">Pricing</a></li>
      <li><a href="#">Docs</a></li>
      <li><a href="#">Blog</a></li>
    </ul>
    <a href="#" class="nav-cta">Dashboard</a>
  </nav>

  <section class="hero">
    <div class="hero-content">
      <h1 class="hero-headline">AI agents that <em>close deals</em></h1>
      <p class="hero-sub">One call. Six agents. Zero setup.</p>
      <p class="hero-body">Record your sales calls and let AI handle the follow-ups, CRM updates, and coaching insights.</p>
      <div class="hero-buttons">
        <a href="#" class="btn btn-primary">Get Started Free</a>
        <a href="#" class="btn btn-secondary">Watch Demo</a>
      </div>
    </div>

    <!-- Grid Animation - ViewBox 700x600, container scales it up -->
    <div class="grid-container">
      <svg class="grid-svg" viewBox="0 0 700 600" preserveAspectRatio="xMidYMid meet">
        <!-- Row 1: 7 cells (x=0 to 700) -->
        <rect class="grid-cell-bg" x="0" y="0" width="100" height="100"/>
        <rect class="grid-cell-bg" x="100" y="0" width="100" height="100"/>
        <rect class="grid-cell-bg" x="200" y="0" width="100" height="100"/>
        <rect class="grid-cell-bg" x="300" y="0" width="100" height="100"/>
        <rect class="grid-cell-bg" x="400" y="0" width="100" height="100"/>
        <rect class="grid-cell-bg" x="500" y="0" width="100" height="100"/>
        <rect class="grid-cell-bg" x="600" y="0" width="100" height="100"/>
        
        <!-- Row 2: 6 cells (x=100 to 700, offset left) -->
        <rect class="grid-cell-bg" x="100" y="100" width="100" height="100"/>
        <rect class="grid-cell-bg" x="200" y="100" width="100" height="100"/>
        <rect class="grid-cell-bg" x="300" y="100" width="100" height="100"/>
        <rect class="grid-cell-bg" x="400" y="100" width="100" height="100"/>
        <rect class="grid-cell-bg" x="500" y="100" width="100" height="100"/>
        <rect class="grid-cell-bg" x="600" y="100" width="100" height="100"/>
        
        <!-- Row 3: 6 cells (x=100 to 700, offset) -->
        <rect class="grid-cell-bg" x="100" y="200" width="100" height="100"/>
        <rect class="grid-cell-bg" x="200" y="200" width="100" height="100"/>
        <rect class="grid-cell-bg" x="300" y="200" width="100" height="100"/>
        <rect class="grid-cell-bg" x="400" y="200" width="100" height="100"/>
        <rect class="grid-cell-bg" x="500" y="200" width="100" height="100"/>
        <rect class="grid-cell-bg" x="600" y="200" width="100" height="100"/>
        
        <!-- Row 4: 6 cells (x=100 to 700, offset) -->
        <rect class="grid-cell-bg" x="100" y="300" width="100" height="100"/>
        <rect class="grid-cell-bg" x="200" y="300" width="100" height="100"/>
        <rect class="grid-cell-bg" x="300" y="300" width="100" height="100"/>
        <rect class="grid-cell-bg" x="400" y="300" width="100" height="100"/>
        <rect class="grid-cell-bg" x="500" y="300" width="100" height="100"/>
        <rect class="grid-cell-bg" x="600" y="300" width="100" height="100"/>
        
        <!-- Row 5: 6 cells (x=100 to 700, offset) -->
        <rect class="grid-cell-bg" x="100" y="400" width="100" height="100"/>
        <rect class="grid-cell-bg" x="200" y="400" width="100" height="100"/>
        <rect class="grid-cell-bg" x="300" y="400" width="100" height="100"/>
        <rect class="grid-cell-bg" x="400" y="400" width="100" height="100"/>
        <rect class="grid-cell-bg" x="500" y="400" width="100" height="100"/>
        <rect class="grid-cell-bg" x="600" y="400" width="100" height="100"/>
        
        <!-- Row 6: 7 cells (x=0 to 700, full width) -->
        <rect class="grid-cell-bg" x="0" y="500" width="100" height="100"/>
        <rect class="grid-cell-bg" x="100" y="500" width="100" height="100"/>
        <rect class="grid-cell-bg" x="200" y="500" width="100" height="100"/>
        <rect class="grid-cell-bg" x="300" y="500" width="100" height="100"/>
        <rect class="grid-cell-bg" x="400" y="500" width="100" height="100"/>
        <rect class="grid-cell-bg" x="500" y="500" width="100" height="100"/>
        <rect class="grid-cell-bg" x="600" y="500" width="100" height="100"/>

        <!-- ==========================================
             ANIMATED PATHS - ALL ON GRID LINES
             ========================================== -->
        
        <!-- Path 1 (mint): Cell1 bottom-right → down → right to Cell2 top-left
             Cell1: (0,0)-(100,100), Cell2: (200,200)-(400,300)
             Route: x=100 down to y=200, then right to x=200 -->
        <path id="path1" class="grid-path" stroke="#46FCAF" 
              d="M 100,100 L 100,200 L 200,200"/>
        
        <!-- Path 2 (purple): Cell2 right edge → right to Cell3 left edge
             Cell2: (200,200)-(400,300), Cell3: (500,300)-(700,400)
             Route: along y=300 from x=400 to x=500 -->
        <path id="path2" class="grid-path" stroke="#A678F4" 
              d="M 400,300 L 500,300"/>
        
        <!-- Path 3 (cyan): Cell3 bottom → down to Cell4 top
             Cell3: (500,300)-(700,400), Cell4: (500,500)-(700,600)
             Route: along x=600 from y=400 to y=500 -->
        <path id="path3" class="grid-path" stroke="#02C0FF" 
              d="M 600,400 L 600,500"/>
        
        <!-- Path 4 (coral): Cell4 left edge → left to Cell5 right edge
             Cell4: (500,500)-(700,600), Cell5: (0,500)-(200,600)
             Route: along y=500 from x=500 to x=200 (FIXED from y=550) -->
        <path id="path4" class="grid-path" stroke="#FF4F5E" 
              d="M 500,500 L 200,500"/>
      </svg>

      <!-- ==========================================
           CELL CONTENT - with icons and more detail
           ========================================== -->
      
      <!-- Cell 1: Row 1, Col 1 (0,0) - 100x100 -->
      <div class="cell mint" id="cell1" style="left: 0%; top: 0%; width: 14.28%; height: 16.66%;">
        <div class="cell-header">
          <div class="cell-icon">
            <!-- Phone icon -->
            <svg viewBox="0 0 24 24" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
            </svg>
          </div>
          <span class="cell-badge">Trigger</span>
        </div>
        <span class="cell-title">Sales Call</span>
      </div>

      <!-- Cell 2: Row 3, Cols 3-4 (200,200) - 200x100 -->
      <div class="cell purple" id="cell2" style="left: 28.57%; top: 33.33%; width: 28.57%; height: 16.66%;">
        <div class="cell-header">
          <div class="cell-icon">
            <!-- Brain/AI icon -->
            <svg viewBox="0 0 24 24" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M9.5 2A2.5 2.5 0 0 1 12 4.5v15a2.5 2.5 0 0 1-4.96.44 2.5 2.5 0 0 1-2.96-3.08 3 3 0 0 1-.34-5.58 2.5 2.5 0 0 1 1.32-4.24 2.5 2.5 0 0 1 4.44-1.04"/>
              <path d="M14.5 2A2.5 2.5 0 0 0 12 4.5v15a2.5 2.5 0 0 0 4.96.44 2.5 2.5 0 0 0 2.96-3.08 3 3 0 0 0 .34-5.58 2.5 2.5 0 0 0-1.32-4.24 2.5 2.5 0 0 0-4.44-1.04"/>
            </svg>
          </div>
          <span class="cell-badge">Processing</span>
        </div>
        <span class="cell-title">recrd analyzes</span>
        <span class="cell-desc">Transcript, sentiment, action items extracted</span>
        <span class="cell-meta">6 agents deployed</span>
      </div>

      <!-- Cell 3: Row 4, Cols 6-7 (500,300) - 200x100 -->
      <div class="cell cyan" id="cell3" style="left: 71.42%; top: 50%; width: 28.57%; height: 16.66%;">
        <div class="cell-header">
          <div class="cell-icon">
            <!-- Chart/pipeline icon -->
            <svg viewBox="0 0 24 24" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <line x1="18" y1="20" x2="18" y2="10"/>
              <line x1="12" y1="20" x2="12" y2="4"/>
              <line x1="6" y1="20" x2="6" y2="14"/>
            </svg>
          </div>
          <span class="cell-badge">CRM Agent</span>
        </div>
        <span class="cell-title">Pipeline updated</span>
        <span class="cell-desc">Deal stage → Negotiation</span>
        <span class="cell-meta">HubSpot synced</span>
      </div>

      <!-- Cell 4: Row 6, Cols 6-7 (500,500) - 200x100 -->
      <div class="cell coral" id="cell4" style="left: 71.42%; top: 83.33%; width: 28.57%; height: 16.66%;">
        <div class="cell-header">
          <div class="cell-icon">
            <!-- Bell/alert icon -->
            <svg viewBox="0 0 24 24" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"/>
              <path d="M13.73 21a2 2 0 0 1-3.46 0"/>
            </svg>
          </div>
          <span class="cell-badge">Risk Alert</span>
        </div>
        <span class="cell-title">Manager notified</span>
        <span class="cell-desc">Budget concern flagged</span>
        <span class="cell-meta">Slack #deals</span>
      </div>

      <!-- Cell 5: Row 6, Cols 1-2 (0,500) - 200x100 -->
      <div class="cell mint" id="cell5" style="left: 0%; top: 83.33%; width: 28.57%; height: 16.66%;">
        <div class="cell-header">
          <div class="cell-icon">
            <!-- Megaphone/marketing icon -->
            <svg viewBox="0 0 24 24" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="m3 11 18-5v12L3 13v-2z"/>
              <path d="M11.6 16.8a3 3 0 1 1-5.8-1.6"/>
            </svg>
          </div>
          <span class="cell-badge">Content Agent</span>
        </div>
        <span class="cell-title">Marketing alerted</span>
        <span class="cell-desc">Competitor quote captured</span>
        <span class="cell-meta">Case study opp</span>
      </div>
    </div>
  </section>

  <div class="theme-toggle">
    <button id="btn-dark" class="active" onclick="setTheme('dark')">Dark</button>
    <button id="btn-light" onclick="setTheme('light')">Light</button>
  </div>

  <script>
    import { gsap } from 'gsap';
    import { ScrollTrigger } from 'gsap/ScrollTrigger';
    import Lenis from 'lenis';

    // Register GSAP plugins
    gsap.registerPlugin(ScrollTrigger);

    // Theme toggle
    function setTheme(theme) {
      document.documentElement.setAttribute('data-theme', theme);
      document.getElementById('btn-dark')?.classList.toggle('active', theme === 'dark');
      document.getElementById('btn-light')?.classList.toggle('active', theme === 'light');
    }
    window.setTheme = setTheme;

    // Initialize Lenis smooth scroll
    const lenis = new Lenis({
      duration: 1.2,
      easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t)),
      smoothWheel: true,
    });

    function raf(time) {
      lenis.raf(time);
      requestAnimationFrame(raf);
    }
    requestAnimationFrame(raf);

    // Calculate actual path lengths for accurate dash animation
    function getPathLength(pathId) {
      const path = document.querySelector(pathId);
      return path ? path.getTotalLength() : 500;
    }

    // Create master timeline that loops
    function createTimeline() {
      // Get actual path lengths
      const path1Length = getPathLength('#path1');
      const path2Length = getPathLength('#path2');
      const path3Length = getPathLength('#path3');
      const path4Length = getPathLength('#path4');

      // Set initial states with actual lengths
      gsap.set('.cell', { opacity: 0, scale: 0.92 });
      gsap.set('#path1', { strokeDasharray: path1Length, strokeDashoffset: path1Length });
      gsap.set('#path2', { strokeDasharray: path2Length, strokeDashoffset: path2Length });
      gsap.set('#path3', { strokeDasharray: path3Length, strokeDashoffset: path3Length });
      gsap.set('#path4', { strokeDasharray: path4Length, strokeDashoffset: path4Length });

      const tl = gsap.timeline({
        repeat: -1,
        repeatDelay: 3,
        onRepeat: () => {
          gsap.set('.cell', { opacity: 0, scale: 0.92 });
          gsap.set('#path1', { strokeDashoffset: path1Length });
          gsap.set('#path2', { strokeDashoffset: path2Length });
          gsap.set('#path3', { strokeDashoffset: path3Length });
          gsap.set('#path4', { strokeDashoffset: path4Length });
        }
      });

      // Cell 1 fades in (the trigger)
      tl.to('#cell1', {
        opacity: 1,
        scale: 1,
        duration: 0.6,
        ease: 'power2.out'
      });

      // Path 1 draws as cell 1 settles
      tl.to('#path1', {
        strokeDashoffset: 0,
        duration: 1.4,
        ease: 'power1.inOut'
      }, '-=0.1');

      // Cell 2 fades in as path arrives
      tl.to('#cell2', {
        opacity: 1,
        scale: 1,
        duration: 0.6,
        ease: 'power2.out'
      }, '-=0.3');

      // Brief pause to read cell 2
      tl.to({}, { duration: 0.4 });

      // Path 2 draws
      tl.to('#path2', {
        strokeDashoffset: 0,
        duration: 0.8,
        ease: 'power1.inOut'
      });

      // Cell 3 fades in
      tl.to('#cell3', {
        opacity: 1,
        scale: 1,
        duration: 0.6,
        ease: 'power2.out'
      }, '-=0.25');

      // Brief pause
      tl.to({}, { duration: 0.3 });

      // Path 3 draws
      tl.to('#path3', {
        strokeDashoffset: 0,
        duration: 0.7,
        ease: 'power1.inOut'
      });

      // Cell 4 fades in
      tl.to('#cell4', {
        opacity: 1,
        scale: 1,
        duration: 0.6,
        ease: 'power2.out'
      }, '-=0.2');

      // Brief pause
      tl.to({}, { duration: 0.3 });

      // Path 4 draws (longest path)
      tl.to('#path4', {
        strokeDashoffset: 0,
        duration: 1.6,
        ease: 'power1.inOut'
      });

      // Cell 5 fades in
      tl.to('#cell5', {
        opacity: 1,
        scale: 1,
        duration: 0.6,
        ease: 'power2.out'
      }, '-=0.4');

      // Hold at end before loop
      tl.to({}, { duration: 1.5 });

      return tl;
    }

    // Start animation when DOM ready
    document.addEventListener('DOMContentLoaded', () => {
      setTimeout(createTimeline, 100);
    });
  </script>
</body>
</html>
