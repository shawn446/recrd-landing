---
import Nav from '../../components/Nav.astro';
import Footer from '../../components/Footer.astro';
import ThemeToggle from '../../components/ThemeToggle.astro';
import BaseHead from '../../components/BaseHead.astro';
---
<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
  <BaseHead title="recrd for Customer Success - Catch Churn Before It Happens" description="Get early warning signals from customer calls, automate QBR prep, and keep accounts healthy with AI-powered insights." />
  <style>
    .hero { padding: 160px 48px 100px; max-width: 1200px; margin: 0 auto; display: grid; grid-template-columns: 1fr 1fr; gap: 80px; align-items: center; }
    .hero-content { max-width: 540px; }
    .hero-badge { display: inline-flex; padding: 8px 16px; background: var(--bg-card); border: 1px solid var(--border); font-family: var(--mono); font-size: 12px; text-transform: uppercase; letter-spacing: 0.1em; margin-bottom: 24px; color: var(--cyan); }
    .hero-headline { font-size: 56px; font-weight: 350; line-height: 1.1; letter-spacing: -0.03em; margin-bottom: 24px; }
    .hero-headline em { font-style: normal; color: var(--cyan); }
    .hero-sub { font-size: 18px; color: var(--text-muted); line-height: 1.6; margin-bottom: 40px; }
    .hero-cta { display: inline-flex; align-items: center; gap: 12px; padding: 18px 32px; font-weight: 500; font-size: 15px; text-decoration: none; position: relative; overflow: hidden; background: transparent; color: var(--text); border: 1px solid var(--btn-outline); }
    .hero-cta::before { content: ''; position: absolute; top: 0; left: 0; width: 0; height: 100%; background: var(--cyan); transition: width 0.3s ease; z-index: -1; }
    .hero-cta:hover::before { width: 100%; }
    .hero-cta:hover { color: #0a0a0a; border-color: var(--cyan); }
    .hero-cta-arrow { display: inline-flex; transition: transform 0.2s ease; }
    .hero-cta-arrow img { width: 16px; height: 16px; }
    .hero-cta:hover .hero-cta-arrow { transform: translateX(4px); }
    .hero-cta-arrow .chevron-hover { display: none; }
    .hero-cta:hover .chevron-default { display: none; }
    .hero-cta:hover .chevron-hover { display: block; }

    .hero-visual { background: var(--bg-card); border: 1px solid var(--border); padding: 32px; }
    .hero-mock-header { display: flex; align-items: center; justify-content: space-between; padding-bottom: 16px; border-bottom: 1px solid var(--border); margin-bottom: 20px; }
    .hero-mock-title { font-weight: 600; font-size: 14px; }
    .hero-mock-badge { font-family: var(--mono); font-size: 10px; padding: 6px 12px; background: var(--cyan); color: #0a0a0a; text-transform: uppercase; }
    .health-row { display: flex; justify-content: space-between; align-items: center; padding: 14px 0; border-bottom: 1px solid var(--border); }
    .health-row:last-child { border-bottom: none; }
    .health-account { font-size: 14px; font-weight: 500; }
    .health-arr { font-family: var(--mono); font-size: 12px; color: var(--text-muted); }
    .health-status { display: flex; align-items: center; gap: 8px; }
    .health-indicator { width: 8px; height: 8px; border-radius: 50%; }
    .health-indicator.healthy { background: var(--mint); }
    .health-indicator.warning { background: var(--yellow); }
    .health-indicator.risk { background: var(--coral); }
    .health-label { font-family: var(--mono); font-size: 11px; text-transform: uppercase; }
    .pain-section { padding: 100px 48px; border-top: 1px solid var(--border); background: var(--bg-alt); }
    .pain-header { text-align: center; max-width: 600px; margin: 0 auto 64px; }
    .pain-label { font-family: var(--mono); font-size: 12px; text-transform: uppercase; letter-spacing: 0.1em; color: var(--coral); margin-bottom: 16px; }
    .pain-headline { font-size: 36px; font-weight: 350; line-height: 1.2; }
    .pain-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1px; background: var(--border); max-width: 1000px; margin: 0 auto; }
    .pain-card { background: var(--bg); padding: 40px 32px; text-align: center; }
    .pain-stat { font-size: 48px; font-weight: 300; color: var(--coral); margin-bottom: 12px; font-family: var(--mono); }
    .pain-desc { font-size: 15px; color: var(--text-muted); line-height: 1.5; }
    .solution-section { padding: 120px 48px; border-top: 1px solid var(--border); }
    .solution-header { text-align: center; max-width: 600px; margin: 0 auto 80px; }
    .solution-label { font-family: var(--mono); font-size: 12px; text-transform: uppercase; letter-spacing: 0.1em; color: var(--cyan); margin-bottom: 16px; }
    .solution-headline { font-size: 36px; font-weight: 350; line-height: 1.2; margin-bottom: 16px; }
    .solution-sub { font-size: 16px; color: var(--text-muted); }
    .solution-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 48px; max-width: 1000px; margin: 0 auto; }
    .solution-card { padding: 40px; background: var(--bg-card); border: 1px solid var(--border); }
    .solution-icon { width: 48px; height: 48px; margin-bottom: 24px; }
    .solution-icon img { width: 100%; height: 100%; object-fit: contain; }
    .solution-card-soon { position: relative; }
    .solution-badge { position: absolute; top: 16px; right: 16px; font-family: var(--mono); font-size: 10px; text-transform: uppercase; letter-spacing: 0.05em; padding: 4px 10px; background: var(--border); color: var(--text-muted); }
    .solution-title { font-size: 20px; font-weight: 600; margin-bottom: 12px; }
    .solution-desc { font-size: 15px; color: var(--text-muted); line-height: 1.6; }
    .quote-section { padding: 100px 48px; border-top: 1px solid var(--border); background: var(--bg-alt); }
    .quote-container { max-width: 800px; margin: 0 auto; text-align: center; }
    .quote-mark { font-size: 80px; line-height: 1; color: var(--cyan); opacity: 0.3; margin-bottom: -20px; }
    .quote-text { font-size: 28px; font-weight: 350; line-height: 1.5; margin-bottom: 32px; }
    .quote-author { font-size: 15px; color: var(--text-muted); }
    .quote-author strong { color: var(--text); font-weight: 600; }
    .cta-section { padding: 120px 48px; text-align: center; border-top: 1px solid var(--border); }
    .cta-headline { font-size: 40px; font-weight: 350; margin-bottom: 16px; }
    .cta-sub { font-size: 18px; color: var(--text-muted); margin-bottom: 40px; }
    .btn { display: inline-flex; align-items: center; gap: 12px; padding: 18px 28px; font-weight: 500; font-size: 15px; text-decoration: none; position: relative; overflow: hidden; background: transparent; color: var(--text); border: 1px solid var(--btn-outline); }
    .btn::before { content: ''; position: absolute; top: 0; left: 0; width: 0; height: 100%; background: var(--cyan); transition: width 0.3s ease; z-index: -1; }
    .btn:hover::before { width: 100%; }
    .btn:hover { color: #0a0a0a; border-color: var(--cyan); }
    .btn-arrow { display: inline-flex; transition: transform 0.2s ease; }
    .btn-arrow img { width: 16px; height: 16px; }
    .btn:hover .btn-arrow { transform: translateX(4px); }
    .btn-arrow .chevron-hover { display: none; }
    .btn:hover .chevron-default { display: none; }
    .btn:hover .chevron-hover { display: block; }

    @media (max-width: 1000px) { .hero { grid-template-columns: 1fr; gap: 48px; text-align: center; } .solution-grid { grid-template-columns: 1fr; } }
    @media (max-width: 900px) { .hero { padding: 140px 24px 80px; } .hero-headline { font-size: 40px; } .pain-section { padding: 80px 24px; } .pain-grid { grid-template-columns: 1fr; } .solution-section { padding: 80px 24px; } .quote-section { padding: 80px 24px; } .cta-section { padding: 80px 24px; } }
  </style>
</head>
<body>
  <Nav />
  <section class="hero">
    <div class="hero-content">
      <div class="hero-badge">For Customer Success</div>
      <h1 class="hero-headline">Catch churn <em>before it happens</em></h1>
      <p class="hero-sub">Stop being surprised by renewals. Get early warning signals from customer calls, automate QBR prep, and keep accounts healthy.</p>
      <a href="#" class="hero-cta">Book a Demo <span class="hero-cta-arrow"><img src="/icons/chevron-green.svg" alt="" class="chevron-default" /><img src="/icons/chevron-black.svg" alt="" class="chevron-hover" /></span></a>
    </div>
    <div class="hero-visual">
      <div class="hero-mock-header"><span class="hero-mock-title">Account Health</span><span class="hero-mock-badge">Portfolio</span></div>
      <div class="health-row"><div><div class="health-account">Acme Corp</div><div class="health-arr">$120k ARR</div></div><div class="health-status"><span class="health-indicator healthy"></span><span class="health-label">Healthy</span></div></div>
      <div class="health-row"><div><div class="health-account">TechStart Inc</div><div class="health-arr">$85k ARR</div></div><div class="health-status"><span class="health-indicator healthy"></span><span class="health-label">Healthy</span></div></div>
      <div class="health-row"><div><div class="health-account">GlobalMfg</div><div class="health-arr">$200k ARR</div></div><div class="health-status"><span class="health-indicator warning"></span><span class="health-label">Monitor</span></div></div>
      <div class="health-row"><div><div class="health-account">StartupXYZ</div><div class="health-arr">$45k ARR</div></div><div class="health-status"><span class="health-indicator risk"></span><span class="health-label">At Risk</span></div></div>
    </div>
  </section>
  <section class="pain-section">
    <div class="pain-header"><p class="pain-label">The Problem</p><h2 class="pain-headline">Churn surprises kill NRR</h2></div>
    <div class="pain-grid">
      <div class="pain-card"><div class="pain-stat">68%</div><p class="pain-desc">Of churn signals are missed in customer calls</p></div>
      <div class="pain-card"><div class="pain-stat">4h</div><p class="pain-desc">Average time to prep for a single QBR</p></div>
      <div class="pain-card"><div class="pain-stat">3mo</div><p class="pain-desc">Too late to save most accounts when red flags surface</p></div>
    </div>
  </section>
  <section class="solution-section">
    <div class="solution-header"><p class="solution-label">How recrd helps</p><h2 class="solution-headline">Account intelligence on autopilot</h2><p class="solution-sub">Know exactly how every account feels—before they tell you they're leaving.</p></div>
    <div class="solution-grid">
      <div class="solution-card">
        <div class="solution-icon"><img src="/icons/alert-ping.svg" alt="Churn Signals" /></div>
        <h3 class="solution-title">Churn Signals</h3>
        <p class="solution-desc">Sentiment shifts, feature complaints, competitor mentions—get alerts the moment an account shows warning signs.</p>
      </div>
      <div class="solution-card">
        <div class="solution-icon"><img src="/icons/meeting-prep-green.svg" alt="Auto QBR Prep" /></div>
        <h3 class="solution-title">Auto QBR Prep</h3>
        <p class="solution-desc">AI-generated account summaries, key milestones, and talking points. Prep that used to take 4 hours, done in 4 minutes.</p>
      </div>
      <div class="solution-card">
        <div class="solution-icon"><img src="/icons/agent-forecast.svg" alt="Expansion Signals" /></div>
        <h3 class="solution-title">Expansion Signals</h3>
        <p class="solution-desc">Not just churn—catch expansion opportunities too. Know when accounts are ready to grow before they ask.</p>
      </div>
      <div class="solution-card">
        <div class="solution-icon"><img src="/icons/champ-map.svg" alt="Stakeholder Tracking" /></div>
        <h3 class="solution-title">Stakeholder Tracking</h3>
        <p class="solution-desc">Know who's engaged, who's gone quiet, and when your champion changes roles. Never be blindsided by a new decision-maker.</p>
      </div>
      <div class="solution-card solution-card-soon">
        <div class="solution-badge">Coming Soon</div>
        <div class="solution-icon"><img src="/icons/checklist.svg" alt="Onboarding Agent" /></div>
        <h3 class="solution-title">Onboarding Agent</h3>
        <p class="solution-desc">Tracks go-live milestones and flags stalled implementations. Know which accounts need a nudge before they go dark.</p>
      </div>
      <div class="solution-card solution-card-soon">
        <div class="solution-badge">Coming Soon</div>
        <div class="solution-icon"><img src="/icons/warning-triangle-red.svg" alt="Escalation Agent" /></div>
        <h3 class="solution-title">Escalation Agent</h3>
        <p class="solution-desc">Detects escalation language, legal threats, and exec involvement. Get instant alerts when conversations get serious.</p>
      </div>
    </div>
  </section>
  <section class="quote-section">
    <div class="quote-container"><div class="quote-mark">"</div><p class="quote-text">We caught a $200k account going sideways two months before renewal. Without recrd, that would have been a surprise churn conversation.</p><p class="quote-author"><strong>David H.</strong> — VP of Customer Success</p></div>
  </section>
  <section class="cta-section">
    <h2 class="cta-headline">Ready to stop surprise churn?</h2>
    <p class="cta-sub">See how recrd gives you early warning on every account in your portfolio.</p>
    <a href="#" class="btn">Book a Demo <span class="btn-arrow"><img src="/icons/chevron-green.svg" alt="" class="chevron-default" /><img src="/icons/chevron-black.svg" alt="" class="chevron-hover" /></span></a>
  </section>
  <Footer />
  <ThemeToggle />
</body>
</html>
